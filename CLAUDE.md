# officraft — builder CLAUDE.md

This file is read by Claude Code agents working in this repo.

## officraft builder 架構憲章 v1

1. **乾淨身分、不借權**:agent/warden 只用自己 scope 的 token 操作 server;遇「向上借權」的設計立刻停下、回報 owner align,不默默接受。owner token 不外借、warden 特權不代勞 agent 個人行為。

2. **對齊 vibe-clicking golden reference——限身分/auth/scope/治理面**:這些面 vibe-clicking(:8766)是成熟參考,officraft 不自創偏離。要偏離先講清為什麼。(注意:presence/status 不在此列——見 §3,那是 officraft 自走的設計。)

3. **presence:即時反映(projection)≠ 狀態,分開存**(officraft 自走設計、非對齊 vibe——vibe 現況不同、未來也許才跟進):online 純粹是 SSE 連線的即時反映(projection;連上=True/斷=False,二態);waking_since / stopping_since 才是衍生 tri-state 狀態的錨點。兩者不同變數、不塞同一欄,presence_state() 是唯一衍生函數。**member 與 warden 同一套 online 判定**——warden 也「自己連上 SSE = online」(owner 2026-07-10 廢除舊「warden 不算 online」);member 起停決策留 server、但**已砍自動 relocate**。權威 logical spec:`docs/design/state-model.md`。

4. **治理層 = {owner, admin_agent} only**(方案 B 語意面已實作完成):改 global context / role、mint 身分 token、bootstrap(mint member JWT 的 seam)這類動作,只有 **owner 或 admin agent** 能執行。⚠️ **lessons 是分級的、不是整條鎖治理層**(owner 2026-07-27 `rc-46599297a1c4` 拍板:**程式現況是對的,改文件對齊**;此前本條把 lessons 與 global context / role 並列,寫窄了):**每個 agent 都可以整理自己那個 role 的 lessons**(`lessonsWriteAuthz` 的 self-role 規則,身分取自 verified sub 的名冊列、不是請求欄位),寫**別人** role 的才需要 admin 以上;READ 不設此限。⚠️ 這句預設了「**有** role」:比對的是名冊列的 `role_key` 對上路徑的 `role_key`,而路徑那個永遠非空,所以**名冊列沒有 `role_key` 的 caller 一個 role 都寫不了**(不是「只能寫自己的」,是零)——外包 worker（`ow-` 是 `member` 表中 `kind=outsource` 的列；`memberFromWorker` 固定其 `role_key=""`）與任何 `role_key=""` 的成員都落在這裡。admin agent = **agent-scope token 且其 member `role_key=="assistant"`**(以 role 為準的判定,不寫死 id;seeded Mira 是第一個實例——舊「sub==mira」hardcode 判法已廢,split-brain 統一到 role 側,見 §14 / `docs/design/caller-identity-convention.md`)。⚠️ **warden 不是治理 principal**(machine class 在階梯最底——warden 零自決 auth,見 §6)。agent 不能自我提權呼叫治理端點;每條路由必須在表上宣告明確 principal 門檻,不靠「agent 不知端點存在」當安全邊界。**把缺口補上**:bootstrap 同鎖 admin 以上,否則任意 agent token 可 `bootstrap {member_id:"mira"}` 拿 admin token 繞過治理 choke;hire 的 kind/role_key 提權面同鎖 admin 以上(否則 agent 可自僱一個 assistant)。

5. **RBAC 統一到單一 resolver + 路由表宣告**:principal 分類統一到 `server/ocserverd/authz.go`(單一 resolver:token scope + member kind/role_key → class `machine < agent < admin_agent < owner`,deny-by-default);每條路由在 route 表(`server/ocserverd/routes.go`)的 `RouteSpec.requires` 宣告最低 class,註冊時掛單一 choke,boot assertion fail-closed 驗每條路由都有宣告。治理寫入 + bootstrap 套 `admin_agent`、mint 套 `owner`,全走這一個 choke,不各自散寫。漏一個宣告 = app 拒起,不是一個裸奔洞。

6. **Spawn / token 權威模型(baseline · 正解 target)**:**server 是唯一 auth 權威**。reconcile 決策該起哪個 member 時,由 **server 自己 mint 該 member 的 token**,並在派工指令當下把 token **隨指令 push 給 warden**。**warden 與 agent 一律零 mint、零自 bootstrap、零自決 auth**:warden 是 stateless 執行手,拿 server 給的 token spawn;agent 拿注入的 token 幹活。⚠️ **現行 `warden-self-bootstrap` pull 路徑是過渡態**——正解 push 路徑的 scaffold 已在碼裡但 dormant(gate OFF),只差 server 端接線通電(P4),pull 路徑 P4 通電後退役。**別拿現行 live pull 碼當設計 target 重建心智模型**——正解是 server-mint → push,不是 warden/agent 自己去要 token。

7. **push 前審完整檔案 manifest**:push 前用 `git diff --stat`(未 commit)/ `git show --stat`(已 commit)把**每一個**新增、刪除、移動的檔逐一過一遍,確認**都有明確必要性理由**——不是只掃「預期改的檔」就放行。⚠️ 這是曾把 `owner_token`(真機密)+ scratchpad 垃圾誤 commit 進 origin/main 的**根因**:當時 review 只審預期檔、對雜檔視而不見,漏網檔就這樣上了遠端。manifest 上任何一個你講不出必要性的檔 = 停下、查清、清掉再 push。CI 第 7 道(path denylist + gitleaks)是硬防線,但**人眼審 manifest 是第一道**,別把它讓給 gate。

8. **context 與碼共存(context lives with code)**:設計/context 文件與其描述的碼 **co-located**——放在一起(模組旁的 doc、或 `docs/` 下對應路徑),讓後人一眼找得到某塊碼的意圖。紀律:(a) **改碼與更新其 context doc 在同一個 commit**,doc 永不落後於碼;(b) **刪 legacy 碼時同一 commit 刪掉它的 context doc**,不留孤兒化石;(c) **動某塊碼前先讀它的 context doc** 建立正確心智模型,改完**同步更新**。理由:過時的 doc/碼會讓後來的 builder 在**錯誤概念**上疊床架屋,愈疊愈歪——主動防之。⚠️ **關鍵護欄(本條只給 doc↔碼 這個特例的判讀,規則本體不在這裡)**:doc 與碼**不一致時,別擅自假設「doc 是舊的」就刪 doc 遷就碼**——很可能是**碼漂離了原始設計、而 doc 才是意圖(design intent)**;git timeline 只當線索、不當判決。**這是「兩份權威互相矛盾」的一個實例,處置一律照 seed 通則辦——`seeds/system_interaction.md` §4.1「第三種開卡理由——兩份權威打架時」是該規則的唯一表述,本條只留上面 doc↔碼 特有的判讀,處置怎麼做請直接讀 seed,本檔不重述也不摘要。** <!-- defers-to: rule:conflicting-authorities@6a56076a857f -->

9. **reviewer code-hygiene checklist(每次 land-flow review 必查)**:review 一個 land 前,除既有 §7 manifest 審查外,逐條過這四點——(a) 有無**該清而沒清的 legacy 碼 / 過時 doc** 被留下?(b) 本次改動有無**建在過時或錯誤的概念**上(疊在已漂移的碼/doc 之上)?(c) 動到的碼,其 **context doc 有沒有隨碼同一波更新**(§8)?**特別地:凡動到 agent 互動面(MCP 工具、`ocagent` CLI、agent 要照做的流程),`seeds/`(global context)必須同一批更新**——agent 只知道 seed 教的做法,seed 不更新=新能力對全 fleet 隱形(owner 定調 2026-07-12;反例:attachment 送端 land 了、seed 卻還教舊法)?(d) 一旦發現 **doc↔碼 misalignment** → 照 §8 的關鍵護欄辦(規則本體見 `seeds/system_interaction.md` §4.1),不自裁哪個對。 <!-- defers-to: rule:conflicting-authorities@6a56076a857f --> 任何一點不過 = 擋下、align 清楚再放行。

---

## 開發約定 (conventions) — 每次 land 都適用

以下 §10–§13 是 repo-wide 的可操作約定,補足 §1–§9 憲章。**各域(server/ cli/ frontend/ conformance/ e2e_test/)另有自己的 `CLAUDE.md`**,記該域專屬慣例、進入該目錄自動 nested-load;本檔只記 repo-wide invariant + convention。CI 對其中可機驗的部分設 gate 硬 enforce(見 §13)。

10. **命名鐵律 folder = go-module = binary 同名**:一個可執行 / 可 import 的單元,其**資料夾名、模組 / 套件名、產出 binary 名三者必須同名**,不用連字號變體或縮寫別名。範例:`cli/ocagent/`(資料夾)→ `module ocagent`(go.mod)→ `ocagent`(binary),不是 `cli/ocagent/`。理由:讓人與 AI 從任一名字都能直接推到其他兩者,消除「folder 叫 A、binary 叫 B」的定位摩擦。⚠️ **介面名已對齊(2026-07-09 owner 定案)**:CLI 的**呼叫子命令名**(spawn 寫的 bare `ocagent` shim)與 **launchd label**(`com.officraft.ocwarden`)已一併統一為 ocagent/ocwarden 命名。它們仍是**獨立的介面契約**(boot-prompt 呼叫名 + launchd 註冊 label,不是 folder/module/binary),改動需 **host 端協調**(重寫 shim / warden bootout+relaunch),不在 folder=module=binary 的 CI gate 覆蓋內(見 §13)。

11. **commit 訊息格式 `[why]` / `[how]`**:每個 commit 用 `[why] <為什麼要改:講清問題 / 動機,讓後人不必考古就懂意圖>` 換行 `[how] <怎麼改的:改了哪些關鍵處>`;body **避免 emoji**;結尾固定一行 `Co-Authored-By: <實際執行的 Claude 模型名> <noreply@anthropic.com>`(例:`Claude Fable 5`、`Claude Opus 4.8`——署真實模型,不為符合慣例署不實名稱)。理由:`[why]` 讓未來的人與 AI 直接讀到改動意圖,對齊 §8 context-with-code。

12. **結構約定**:
    - **路由 table-driven**:新增 server 端點 = 在 route 表(`server/ocserverd/routes.go`)加一行 `RouteSpec` + handler + test,不散寫 mount(且 wire 已凍結——先走 spec,見 §13)。
    - **DTO 向後相容**:對外 DTO 加欄一律 `optional`(不破既有 client);要破壞相容**先問 Seth**(同源於「該停下來問 owner、不自行拍板」那一類;規則本體見 `seeds/system_interaction.md` §4.1,§8 是它在 doc↔碼 上的實例)。

13. **land / verify 紀律(AI-friendly:宣告前先查證)**:
    - **verify-at-source > 自報**:任何「做完了 / 能跑 / 現況是 X」的 claim,回**最源頭**查證(git / curl / CI 實際輸出),不憑記憶或 exit code。CI 綠的判準是 **rc == 0 且整份輸出的最後一行**精確等於 `[ci] all green`(`tail -n 1 | grep -qFx '[ci] all green'`)——**兩個條件都要,寬鬆 grep 一律不算**。⚠️ 這條在 T-d3e3 從「只看最後一行」改成 AND,理由是兩半各自都不夠:step 0 的 `tests_guard` 第一步就印自己的 `all green`,所以任何爆掉的 log 都已含該子字串(寬鬆 grep 無效);而**被 dispatch 的 lane 只要 `echo "[ci] all green"; exit 1`,ci.sh 的 `set -e` 就在那裡中止、偽造權威剛好落在最後一行**(這個假綠被實作出來過)。舊文寫「不是 exit 0」講的是 **rc 不足以單獨判綠**(前例:`bin/common.sh` 的 `set -e` 打敗 `run_all.sh` 刻意的 rc 捕獲,失敗訊號默默消失),**不是「rc 不該被檢查」**——要求兩者同時成立比任一半都嚴格,與原裁定相容。可執行形式見 `bin/tests/ci-success-marker.sh`(同時掃 ci.sh 與每個被 dispatch 的 lane 腳本)。⚠️ 連自跑 CI 也可能被 **working tree(含未 commit 的 fix)**騙——land 前確認 **working tree == commit**(`git status` clean),驗的是 commit 不是 tree。
    - **recon 一律基於 `origin/main`**:此 repo 的 `local main` 是條**孤兒 old-flat-layout 歷史**(與 origin/main 無共同祖先),讀它 = 全錯 layout。verify-at-source 的 source = `origin/main`。
    - **wire 已凍結(M1)**:動 wire(HTTP OpenAPI 面或 MCP tool 面)= **先改 `spec/*.json`(spec/openapi.json / spec/mcp-catalog.json)+ owner 過目,再動碼**;ci wire-freeze gate(step 1g gen-ocapi drift:committed `ocapi_gen.go` 必須從凍結 spec byte-identical 重生 + step 4 FE schema drift;MCP 描述子由 ocserverd 直服凍結 `spec/mcp-catalog.json`,by construction 不漂)會擋任何未過 spec 的漂移;行為面由 `conformance/run.sh --target go` 收官。
    - **land pipeline(走 PR,不是直推 `main`)**:off `origin/main` 開分支 → 隔離 worktree、標死 scope → 親驗全 diff(無 scope-creep)+ 親讀關鍵邏輯碼 → `bash bin/ci.sh` **rc == 0 且最後一行**精確為 `[ci] all green` → push **分支**(`git push origin <b>`——⚠️ **push 被以 *OAuth App … without `workflow` scope* 之類的訊息退件時,那是憑證管道的問題、不是你改的東西有錯**:HTTPS 的 token 沒有動 `.github/workflows/` 的權限。動作是把 `remote.origin.pushurl` 指到 SSH 再推,**不要**為了閃過它去改動或還原你的 workflow 編輯。這裡寫的是**症狀→動作**,不是哪台機器現在設成什麼——後者是會過期的機器狀態,不屬於這份文件)→ **讀回確認**(`git ls-remote origin <b>`——push 沒報錯不等於推上去了)→ `gh pr create` 開 PR → 等 PR 上的 check 都綠(`.github/workflows/ci.yml`,`pull_request` **與 push-to-`main`** 兩個觸發——post-merge 那一輪由 T-ab2a 補上,`main` 上不 cancel-in-progress。⚠️ **「兩個觸發跑同一組 job」已經不字面為真了**(舊文就是這樣寫的):`main` 上有**兩個** main-only job——`notify-main-red`(T-5d3b,只在有 job 失敗時跑)與 `auto-beta`(T-9fe3,綠了就自動發一顆 beta prerelease)。仍然成立、也是這條規則真正在保護的那半是:**`main` 不會跑任何 PR 跑不到的「檢查」**——那兩個 job 都不檢查任何東西。這件事不靠自律:`bin/tests/auto-beta-guard.sh` 要求 `auto-beta` 的 `needs` **恰好等於 gate job 的集合**(兩個方向的差集都要空,新增一個 gate 而漏進 `needs` 會紅,把非 gate 塞進去也會紅),而**哪些 job 是 gate 是每個 job 自己在 `ci.yml` 裡用 `# oc-job-role:` 標記宣告的、由守衛強制**:沒標、標兩次、標一個讀不懂的值,守衛會 **FAIL 並點名那幾行**,不會把它默默歸進任一邊;標記本身也要被佐證(宣告非 gate 就必須把 `if` 釘在 `refs/heads/main`;宣告 gate 的 job 層 `if` **只能是空的、或恰好等於守衛裡那份 `ALLOWED_GATE_IF` 點名清單的某一條**——⚠️ 舊文只寫「不准釘 main」而守衛也只認 `github.ref == 'refs/heads/main'` 那**一種**拼法,換成 `github.ref_name == 'main'` 或 `startsWith(github.ref, …)` 就把一道真閘從 PR 上拿掉而守衛全綠(實測過兩顆 mutant);於是改成「閘一個 `if` 都不准有」,那又**過強**——`github.event.pull_request.draft != true` 這種完全合法的 skip-draft-PR 也被打紅(實測 35 ok / 2 failed)。現在的形狀是**白名單**:沒列到的條件一律紅,不管它是不是繞法,要放行就得把那一條逐字加進守衛、而那個編輯就是審查。⚠️ **「逐字等於清單某一條」以前只在紙上為真**:那份清單本來寫成加括號的 tuple 而且是**空的**,於是那道門一次都沒被執行過——而 Python 的經典陷阱是單一條目**漏掉結尾逗號**就不是 tuple 而是 **str**,`x in "字串"` 是**子字串包含**不是相等。審查實測:清單放一條看似合理的條目、gate 掛 `if: github.ref == 'refs/heads/main'`(**就是這整張票起因的那個繞法**),守衛 **39 ok / 0 failed** 放行。現在容器型別本身被釘死(`frozenset` ＋ 元素形狀檢查),漏逗號會**當場紅並說明原因**,而不是安靜降級。⚠️ 白名單只值它被讀過的那一次:清單上的條件是被**完全信任**的,機制逼出討論、不代替討論。比對前會正規化空白與 `${{ }}` 外殼,其餘逐字比;**引號不正規化**——雙引號會被逐一點名拒絕,理由見下一段的 GitHub 實測)。**`auto-beta` 的 `if` 也不再是「兩個子字串都在就算」而是與規範形式逐字元相等**(舊寫法只做 substring 比對、不看布林結構,`(<規範式>) || github.event_name == 'pull_request'` 兩個子字串都還在 ⇒ 守衛全綠、而唯一持有 `contents: write` 的 job 會在 PR 上跑、把未合併的碼發成真的 prerelease);⚠️ 🔴 **舊文寫「`== "push"` 與 `== 'push'` 是同一個條件」,那是假話,而且是這族裡最貴的一種**——它會教下一個人寫出一個**讓整份 workflow 不啟動**的條件。**GitHub 實測**(主 agent 在丟棄式分支上跑兩份最小 workflow、只差引號;分支與 worktree 已刪、`ls-remote` 回 0 行):單引號 `if: github.event_name == 'push'` ⇒ **run 成功、`name:` 正常解析**;雙引號 `== "push"` ⇒ **startup failure、`jobs: []` 零個 job**,run 名稱退回成檔案路徑(＝那份 workflow 從沒被解析、`name:` 沒被讀到)。所以雙引號**不是同義改寫,而是一份不會被排程的 workflow**,而它在 PR 上的樣子是**零個 check**(不是紅、沒有結論),比紅還難發現。⚠️ 因此第二輪那個「把雙引號正規化成單引號」在雙引號這一格上是**放寬、不是修 bug**;守衛拒絕雙引號本來就是**對的**,舊文記的「實測 36 ok / 1 failed 是誤紅」該讀成**真紅**。現在:**正規化只做空白與 `${{ }}` 外殼**(那兩個確實同義),**雙引號一律拒絕並在訊息裡講明原因**(不合法、整份 workflow 不啟動、PR 上是零個 check),**布林結構仍然逐字比**(換值、加子句、加否定照樣紅——實測過)。射程覆蓋**每一個宣告了 role 的 job 的 job 層 `if`**,不只 `auto-beta`:舊版的 not-a-gate 釘樁 regex `['\"]` 兩種引號都收,所以 `github.ref == "refs/heads/main"` 會被讀成「釘對了」而其實零個 job 會跑。三顆 mutant 實測(都 rc=1):`auto-beta` 的 `if` 改雙引號 ⇒ **40 ok / 3 failed**(W1r 與 W2 都點名理由);gate 掛 `if: github.event_name == "pull_request"` ⇒ **41 ok / 2 failed**;not-a-gate 的釘樁改雙引號 ⇒ **41 ok / 2 failed**。**前一版守衛(`081d337`)對後兩顆與第一顆全部放行**(0 failed)——實測過。⚠️ 🔴 **射程後來又擴了一次,而擴的理由是「照後果劃、不照層級劃」**:初版只讀 job 層 `if`,沿用的理由是「step 層 `if` 不能 skip 整個 job」——那句對 skip 為真,**對這件事無關**,因為雙引號不是 skip,是**整份檔不啟動**。主 agent 第二輪 GitHub 實測(三份最小 workflow,分支與 worktree 已刪、`origin/main` 未被觸碰):**step 層 `if` 寫雙引號 ⇒ startup failure、`jobs` 數 = 0**;**`${{ }}` 內部寫雙引號**(`env: WHO: ${{ github.event_name == "push" }}`)**⇒ startup failure、`jobs` 數 = 0**;**對照組兩處都用單引號 ⇒ success、`jobs` 數 = 1 且 job conclusion=success**(對照組真的有 job 跑完 ⇒ 那兩個 failure 不是環境問題)。所以規則是**雙引號出現在 expression 裡就拒絕,不分它住在哪一層**。⚠️ **實測到的位置是那三格**(job 層 `if`、step 層 `if`、`${{ }}` 內插);守衛的掃描是走**整份解析後的文件**、by construction 會碰到其他 expression 位置,但**「GitHub 會怎麼處理」這個斷言只到那三格**,其餘位置**未實測**——別把這段讀成「所有欄位」那種全稱句。⚠️ **反向的誤叫防線同樣被釘住,而且有必須維持綠的正向樣本**:`run:` 裡的 shell 雙引號(`run: echo "hi"`)、以及普通 YAML 字串值(`env: FOO: "bar"`、`with:` 的引號 scalar)**都合法、一律不得判紅**——普通 YAML 值這一格其實是 parser 免費給的:引號在 YAML 解析時就被吃掉了,能活到守衛眼前的 `"` 才是 expression 裡那種。守衛裡的 **W1rq / W1rq-neg** 兩顆用植入 fixture 把兩個方向都釘住:必紅那份要求**三格全中**(實測 `3`,訊息逐格點名),必綠那份要求**一個都不許紅**(實測 `0`)。實測數字:step 層 mutant ⇒ **43 ok / 2 failed / rc=1**(點名 `job 'cloud-gates' step 1 … step-level if`);`${{ }}` 內插 mutant ⇒ **43 ok / 2 failed / rc=1**(點名 `jobs.cloud-gates.steps[0].env.WHO`);把合法雙引號(一顆 plain YAML scalar ＋ 一顆 `run:` shell 引號)加進真的 `ci.yml` ⇒ **45 ok / 0 failed / rc=0**(維持綠)。**前一版守衛(`fbe90c8`)對那兩顆 mutant 全部放行(0 failed)**——實測過。⚠️ 🔴 **第四輪獨立審查抓到我們自己造的誤叫,已修——而且那句「雙引號在 Actions expression 裡不合法」本身就是超射程全稱句**:偵測寫成**字元存在性**(`'"' in …`),而 expression 的字串字面值是用**單引號**界定的、單引號字面值內部可以放任意字元,所以 `if: contains(github.event.head_commit.message, '"skip-ci"')` 這種**完全合法**的條件被判紅(審查實測 43 ok / 2 failed;我自己再驗前一版 `e2c0ae2` 對這條也是紅 ⇒ **42 ok / 2 failed**)。**這正是本包最初要修的病**:守衛誤叫 → 大家學會無視守衛。現在判的是**「雙引號有沒有落在字串分隔符的位置」**(逐字元掃、認得單引號字面值與 `''` 轉義),**單引號字面值內部的雙引號一律放行**;訊息與本段敘述同步收窄成**只講分隔符位置那一格**。⚠️ **證據要分開讀**:「雙引號當**分隔符**會讓整份 workflow 不啟動」是 GitHub 實測;「雙引號在**單引號字面值內部**沒事」**沒有在 GitHub 實測**——那是讀 expression 文法得到的,審查實測到的只是「我們的守衛拒絕了它」,不是 GitHub 怎麼處理它。修後實測:那條 `'"skip-ci"'` 條件加進真的 `ci.yml` ⇒ **45 ok / 0 failed / rc=0**(必綠),而三顆必紅一顆都沒退化——`auto-beta` 的 `if` ⇒ **42 ok / 3 failed**(W1r/W1/W2)、step 層 ⇒ **43 ok / 2 failed**、`${{ }}` 內插 ⇒ **43 ok / 2 failed**,全部 rc=1。⚠️ 附帶一個**只在這裡記一次的教訓**:W1rq 那顆對照用字串去數守衛訊息裡的命中,訊息改寫後那個 matcher 沒跟著改,兩邊計數都變 0、W1rq 當場紅並說「規則什麼都沒抓到」——**紅的形式是對的**(對照停止匹配不可以看起來像通過),但它指的是 matcher 不是規則;要改訊息就同一個 edit 改 matcher。⚠️ **另外還有一層過去整份守衛都沒讀:`on:` 區段**。`on.pull_request.paths-ignore: ['**']` 一行就讓**每一道閘在每個 PR 上都不出結論**,而守衛 37 ok / 0 failed 全綠(實測)。⚠️ **舊文接著寫「閘會不會被排程是 `on:` 決定的,不是 job 裡任何欄位」,那句是假的**——實查守衛全檔:`concurrency` 命中 **0**、`strategy` 命中 **0**(陽性對照:`needs` 命中 **21**,證明查法有鑑別力)。照實況寫:**守衛今天讀的欄位**恰好是 `on:`(W7)、job 層 `if`(W1/W1r/W2)、`needs`(W1)、`permissions`(W3)、job 層 `continue-on-error`、以及 `# oc-job-role:` 標記。**殘餘——三條讓閘不出結論而守衛全綠、今天沒有守衛看著的路**(要不要補進守衛是這包**範圍外**、owner 手上有卡在裁):①把某個 gate 的 `needs` 指向一個在 PR 上會 skipped 的 job,依賴者跟著 skipped(**未實測**);②`strategy.matrix` 的 `exclude` 把某個 gate 清成零個 job(**未實測**);③`concurrency`——`ci.yml` 自己的註解就寫著 `main` 上 `cancel-in-progress: false`、group 是 `ci-${{ github.ref }}`,而 pending 只有一個名額,被擠掉的那顆 commit conclusion 是 `cancelled`(不是紅)⇒ **沒有 verdict 也沒有 beta**(引自 `ci.yml` 註解,**未在 GitHub 實測**)。現在 W7 讀 `on:`,形狀**與 gate 的 `if` 白名單對稱**:**被釘的 trigger(`pull_request`、`push`)其值逐字釘死**(不是「有沒有 pull_request 這個 key」——那看不見旁邊多出來的 filter;所有 suppressor 都住在它所修飾的那個 trigger 底下,所以釘值才擋得住 `paths-ignore: ['**']` 與 `branches: [release]`),而**白名單上的 trigger key 可以直接加、不必動守衛**(今天名單上是 `workflow_dispatch`)。放行 key 安全的理由不是「它看起來無害」,但也**不是**舊文那句全稱的「一個 trigger 不會過濾另一個 trigger」——那句在它沒考慮的方向上是假的。射程只到**狹義版**:**加 `workflow_dispatch` 沒辦法讓 gate 在 `pull_request` 上不跑**,因為 PR 那一輪的 `github.ref` 不同、concurrency group(`ci-${{ github.ref }}`)因此也不同,手動 run 擠不到 PR 那一格(**推論自 `ci.yml` 的 group 運算式,未在 GitHub 實測**);而 `auto-beta` 的 `github.event_name == 'push'` 那半另外擋住手動觸發去發版。⚠️ **`main` 那半不成立**:`main` 上 `cancel-in-progress: false`、pending 只有一個名額,所以**一個手動觸發、打在 `main` 上的 run 可以擠掉正在排隊的那顆 commit**,而被擠掉的那顆 conclusion 是 `cancelled` ⇒ 沒有 verdict、也沒有 beta(**引自 `ci.yml` 自己的 concurrency 註解,未在 GitHub 實測**)。名單外的 trigger(`schedule`、尤其 `pull_request_target`——它拿 write token 跑 fork 的碼)一律紅,要放行就得加進守衛那一行,而那個編輯就是審查。⚠️ **初版把整個 `on:` mapping 逐字釘死,那是過強**:審查實測加一個 `workflow_dispatch` 會 38 ok / 1 failed,而那是合法編輯——同一份守衛對 gate `if` 開了門、對 `on:` 開牆,沒有理由。⚠️ 白名單與 roll-call 的老問題照樣在:**名單上的 key 被完全信任、而且它的值根本不會被讀**。**另外:任何 job 都不准帶 job 層的 `continue-on-error`**(守衛從前整檔不讀這個欄位):它讓失敗的 job 對外報 success ⇒ 同時滿足 `auto-beta` 的 `needs`(紅的主幹照樣發版)並讓 `notify-main-red` 的 `failure()` 為假(沒人被通知),兩道防線一起靜默失效;要讓某個 step 容錯就把欄位放在那個 **step** 上。⚠️ `auto-beta` **不 `needs` `notify-main-red`**,反向才有邊(`notify-main-red` needs `auto-beta`):notify 只在失敗時跑,等它會讓 `auto-beta` 永遠不跑;兩邊互等就是 `needs` 循環,而 GitHub 對循環的回應是**整份拒絕、零個 job 被排程**。三個 gate job 是:**`cloud-gates`** 跑 `bin/ci-cloud.sh`,乾淨 Linux 的 cross-check;**`macos-e2e`**(T-ff8a 新增)在 macOS runner 上跑 `e2e_test/run_all.sh` 的瀏覽器情境——**它什麼旗標都不用設**(T-c329:要活的 agent 那一類 spec 預設就不跑,雲端不必「記得排除」;要跑得自己帶 `OC_E2E_LIVE_AGENT=1`,而那會 spawn 真 agent、燒真 API 額度);**`macos-host-gates`**(T-ab2a 新增)在 macOS runner 上跑 `bin/ci-macos-host.sh`——那幾塊只跑在一台開發機上的閘(`bin/tests/run.sh`、內容層 gitleaks、TCC 身分錨點比對,以及 T-0fef 起的 `frontend` `npm run test:ct` 真瀏覽器 CT + paint 守衛;**塊數同樣會變,要知道跑什麼就讀那支腳本**)。⚠️ **不要數數字**:job 數量還會再變(這句話本身在 T-ff8a、T-ab2a、T-5d3b、T-9fe3 各過期一次),判準是「PR 上**每一個** check 都有結論,且**除了豁免名單上的 job 之外**,其餘每一個都是 success」。⚠️ **豁免名單是一份寫死的點名清單,目前有兩個成員:`notify-main-red`(T-5d3b)與 `auto-beta`(T-9fe3)。** 資格判準只有這一句、沒有第二個版本:**該 job 本身不是閘(不驗任何東西、不產生 land 訊號),存在的目的就是在 `main` 上通知或發版**。⚠️ **`auto-beta` 這個豁免是它自己的裁定(owner 2026-08-05 兩條裁定 + T-9fe3),不是引用 `notify-main-red` 那條類推**——那條自己就寫著「要再加第二個豁免,得有它自己的裁定」,這裡就是那個裁定;要加**第三個**同樣得有自己的,不能引用這兩條之一。⚠️ **這份名單本身現在是被強制的,不再只是散文**:`bin/tests/auto-beta-guard.sh` 的 W1x 要求「宣告自己不是閘」的 job 集合**恰好等於這兩員**,出現第三個就 FAIL 並點名它宣告在第幾行(即使它老實標了 not-a-gate、`if` 也釘對了),訊息直接寫「要有自己的裁定,而且請連同這道守衛一起改」。這裡刻意用寫死的點名清單,跟本檔別處反對的「會過期的名字清單」不同:那種清單**會安靜地過期**,而這一份**只能靠改它本身**繞過,而那正是這條裁定要求的「有意識的動作」。**「掛了 `if` 所以在 PR 上不跑」不是資格**——給一道真的閘加上 `if:` 讓它在 PR 上跳過,它仍然是閘、仍然要 success,而那正是這條規則要抓的東西:一個**本該跑卻沒跑**的 gate 與 `check-runs` 回空同類——`check-runs` 回空等於從沒跑過,畫面上跟「沒紅」一模一樣,而沒跑的 gate 一樣難分。`notify-main-red` 的實際樣子(**實測**,PR #126 / `753f229`,2026-08-05):它的 `if` 在 `pull_request` 上恆假,GitHub 給的 conclusion 是 **`skipped`**,同一輪 `cloud-gates` 與 `macos-e2e` 為 `success` ⇒ **每一個 PR 從此都會帶著一個「有結論、但不是 success」的 check**。`auto-beta` 在 PR 上同樣不會跑(`if` 的兩個條件在 `pull_request` 上都假)——**實測**(PR #127 / `dde6e84a`,2026-08-06):conclusion 是 **`skipped`**,同一輪三個 gate job 皆 `success`、`notify-main-red` 亦為 `skipped` ⇒ **每一個 PR 從此會帶著兩個「有結論、但不是 success」的 check**,不是一個。**它們全部都不是** land 權威,**land 權威仍是本機 `bin/ci.sh`**)→ **合併前先 rebase 到最新 `origin/main`**(`git rebase origin/main` + `git push --force-with-lease`,並重跑 `bin/ci.sh` 到綠——rebase 後的樹沒被驗過)→ 合併 PR(**`gh pr merge --merge`**——⚠️ **方法要明講,不能讓它預設**:`--rebase` / `--squash` 都會**重寫 SHA**,於是「我在本機跑過完整 `bin/ci.sh` 的那一顆 commit 是 `main` 的祖先」這句話**構造上就不再為真**——被驗過的那顆 SHA 根本不在主幹的歷史裡,land 權威跟主幹之間的那條線斷掉,而畫面上一切正常。合併後那個 `git merge-base --is-ancestor <本票 sha> <主幹 sha>` 的判定就是這件事的可執行形式:用錯方法的人會**當場**撞到它為假,而不是三天後才發現手上那份 CI 證據指著一顆不存在於主幹的 commit)→ 雙源查證(`git ls-remote origin main` + `gh api repos/pkyosx/OffiCraft/commits/main --jq .sha`)→ 收退路(`worktree remove` + `branch -D`)。
      ⚠️ **`main` 是受保護分支,直推(`git push origin <b>:main`)會被 `protected branch hook declined` 擋掉**——本條在 T-6d74 從「FF fast-forward push 進 main」改寫成 PR 流程,就是因為舊文寫的那個動作照做必定失敗,而實際 land 一直走 PR(`git log --merges` 上的 `Merge pull request #NN` 就是證據)。**保護不是要繞過的對象,也不是可以放寬的旋鈕**:不要 `--admin`、不要改保護設定,也不要為了讓某道 check 過而放寬它。舊文用 FF push 換來的「主幹上沒有未經驗證的意外交錯」,現在由上面那個 **rebase-to-latest-main + 重跑 CI** 的步驟承接;合併本身在 GitHub 端產生 merge commit(現況即如此),不強求線性歷史。
      ⚠️ **land ≠ 上站(但 T-9fe3 起不再是「完全不會」)**:站上跑的是 **GitHub release**,autodeploy 只追最新 release(`server/ocserverd/auto_update.go`),**不追 `main`**——這條沒變。變的是誰去發:**push 到 `main` 那一輪 check 全綠,`ci.yml` 的 `auto-beta` job 會自動發一顆 beta prerelease**(owner 2026-08-05 兩條裁定:發 beta 自動化、**GA 維持人工**)。⚠️ **「自動發 beta」≠「站會自動換過去」,這條讀寬了會害人**:自動發出來的是 **prerelease**,一個站要真的吃進它,得**兩個開關同時開**——`updater.receive_beta` 與 `updater.auto_update`(**兩個的常數、struct 欄位與 `getBool` 預設都在 `server/ocserverd/settings.go`**,DB 沒那一列時不寫 dst ⇒ 維持 bool 零值 `false`;`auto_update.go` 是那個迴圈,不是預設值所在)。**預設兩個都是關的**,所以對一個預設狀態的站來說,「land ≠ 上站」**一個字都沒變**。owner 自己那台兩個都開著 ⇒ **他那台**會自動吃進每一顆 beta、而且**沒有退版按鈕**(他已知情接受,所以自動路徑刻意不加保護/節流/確認閘)。⚠️ **紅的那一輪不等於「什麼都沒發出去」**(本條初版寫成「連 beta 都不會存在」,是假的):gate job 紅 ⇒ `auto-beta` 不跑、確實沒有 beta;但 **`auto-beta` 自己跑到一半失敗就不一樣**——`bin/release` 第 6 步 `gh release create` 在第 7 步回讀**之前**,而全檔**沒有任何 rollback**(`release-guard.sh` 的 E2「…and it did upload first」就是專門釘這件事)。所以 upload 之後才失敗會留下一顆**沒通過回讀的 prerelease**,而且沒人回收它:存成 draft 的無害(站台 admission 會濾掉),但 **asset 不全的那種站台看得見**(admission 只看 draft/prerelease、不檢查 asset)。清理是**人工動作**,見 `docs/dev/README.md`。**GA(Latest 指向)一律人工**:`bin/release` 的 beta→final 子命令只由人跑,任何 workflow 都不准提到它,`bin/tests/auto-beta-guard.sh` 另外擋掉會搬動 Latest 指向的那組 `gh release edit` 旗標——但**那是兩個具體形狀,不是「GA 不可能被自動化」的證明**(workflow 仍可經由它呼叫的腳本、`gh api` 或 REST 繞過)。⚠️ **這兩條 grep 的作用域是整個 `.github/`,不只 `.github/workflows/`**:舊版只掃 workflows 目錄,於是一個 `.github/actions/<name>/action.yml` 的 composite step 跑 `bin/release promote` 就完全看不見(實測 mutant 守衛全綠),而那個 step 就跑在唯一持有 `contents: write` 的 job 裡。現在 W4/W4b 掃整個 `.github/`,W4pc 用植入的樣本證明那兩條 grep 是活的(否則「零命中」與「grep 壞了」長得一樣)。⚠️ **但「掃整個 `.github/`」並沒有把 composite action 這條路關掉,別讀成關掉了**(本檔上一版就是這樣寫的):local action **不必住在 `.github/` 底下**——repo 根目錄一份 `action.yml` 配 `uses: ./`、或 `tools/finalise/action.yml` 配 `uses: ./tools/finalise`,GitHub 照跑,而 W4/W4b 的作用域根本掃不到它(**實測 37 ok / 0 failed 全綠**)。同理,舊的 W4d「直接禁止 `.github/actions/` 目錄存在」也是**釘錯靶**:付了「不准用 composite action」的代價,卻只擋住那一個目錄。現在 W4d 釘的是**引用**而非位置:**`.github/` 底下任何 YAML 檔的 `uses:` 都不得指向本 repo 自己的碼**——兩種寫法都算:相對路徑(`./…`、`../…`)、以及用 `{owner}/{repo}/{path}@{ref}` 形式**指名本 repo 自己**(`pkyosx/OffiCraft/tools/finalise@main`)。而且**判讀的是解析後的文件、不是逐行 grep**:`uses:` 的值寫在**下一行**是合法的 YAML plain scalar,逐行的 regex 天生看不見它。⚠️ **這三件事(換行寫法、`{owner}/{repo}` 自指、以及原本那句「`./…` 是唯一入口」)是實測打回來的**:前一版守衛寫著「那是任何 local action 被叫到的**唯一入口**」,而審查用這兩種寫法各塞了一個跑 `bin/release promote` 的 composite action 進 `contents: write` 的 job,守衛 **39 ok / 0 failed 全綠**。**別再用絕對詞寫這一段。** W4dpc 用六個植入樣本(含上述兩種)證明掃描活著、並要求它**不要**誤判兩個真的第三方 action;W4dpf 證明「解析不了的檔案」會回報成失敗而不是被跳過;W4ds 用 `git remote` 佐證那個寫死的 repo slug 還是這個 repo——⚠️ **它有三種結果、中間那種是新加的**:origin 解析得出 `{owner}/{repo}` 就逐字比對(不符=紅,實測 mutant 把常數改成 `pkyosx/SomeOtherRepo` ⇒ 42 ok / 1 failed / rc=1);**解析不出**(沒有 git metadata,**或 origin 是檔案路徑——那就是 `bin/lib/ci-lock.sh` 明文教的「另 clone 一份跑 CI」那個 supported 流程**)則印一行 `⚠️ NOT CORROBORATED` 並單獨計數,**不計入 fail、rc 仍為 0**(實測 local clone 與 `git archive` 兩條路都是 42 ok / 0 failed / 1 not corroborated / rc=0)。⚠️ **這是刻意選的方向,代價寫在這裡**:上一版兩條路都判 fail,結果是 `bin/tests/run.sh` 紅、`bin/ci.sh` 在任何本機 clone 裡**永遠到不了 `[ci] all green`** —— 守衛擋死自己 repo 宣告 supported 的流程;換來的代價是**在 origin 不是 GitHub URL 的樹裡,改名/複製到別的 repo 不會紅**(那條殘餘列在下面 (a))。⚠️ **這個曝險比「不算 fail」聽起來窄,界線要講清楚**:會走到「無從佐證」那一格的**只有本機執行**——origin 是檔案路徑的 clone、或完全沒有 git metadata 的樹。**CI 上不是那一格**:runner 的 `actions/checkout` 留下的 origin 幾乎必然是 `https://github.com/…`,所以**雲端那幾輪走的是「逐字比對、不符就是真紅」那條**(mutant 實測過)。所以這是一個**本機執行姿態**、不是雲端的洞;它留下的殘餘精確地只有一件:**改名或把這支檔複製到別的 repo,而且沒有任何人在有 GitHub origin 的地方跑過它**。⚠️ **它現在擋不住的,逐條**:(a) 第三方 action(別人的 repo)、或本 repo 被**另一個 slug** 引用(fork、或改名後沒同步更新守衛裡的常數;**而 origin 產不出 `{owner}/{repo}` 的樹連 slug 佐證都拿不到**——沒有 git metadata,或 origin 是檔案路徑的本機 clone;那種情況印 `⚠️ NOT CORROBORATED` 但不紅);(b) 掃描**走不到**的檔案裡的 `uses:`——副檔名不是 `*.yml`/`*.yaml`,或藏在一個 **symlink 目錄**底下(ruby 的 `**` 不穿 symlink;這兩點都沒實測)——那只剩 W4dt 那層逐行 grep,而它對換行寫法照樣是瞎的;(c) `uses: ${{ … }}` 這種 run time 才組出來的引用,守衛只看字面。⚠️ **它仍然不是「GA 不可能被自動化」**:ci.yml 裡一個 `run:` step 可以呼叫 repo 內任何腳本,那支腳本想做什麼都行。W4/W4b/W4d 蓋住的是「**多開一個 step 的家**」這一類形狀,不是全稱命題。手動發版流程仍在(`bin/release publish --beta` → beta→final 子命令;⚠️ **`publish` 會先在那顆 target commit 的乾淨 staging worktree 裡跑一次完整 `bin/ci.sh`,不綠就不發、沒有跳過開關**——T-b65e / owner 卡 `rc-ffb4b06ad1d9`,細節見 `docs/dev/README.md`〈發版指令〉),並以 `/api/version` 的 `git_sha` 做**祖先判定**坐實已服役(公開免 auth;`version` 欄自建是 `0.0.0`、官方包由 `bin/release` 蓋成真實版號,所以對賬一律看 `git_sha`)。⚠️ **agent 不要手刻 curl 去問它**(開機說明 §6 的白名單不含 server API,而且沒有「唯讀所以無害」的例外):T-77b4 起同一份回應是 MCP 工具 **`get_version`**(零參數,`/api/version` 那一列從 `MCPExclude` 改成掛上工具面;因為那一列是 `requiresPublic`,**任何已認證成員都呼叫得到**)。**別跟 `check_release` 混為一談**——那支問的是「GitHub 上有沒有更新的版本」(`requires=admin_agent`),與「我這台現在跑哪一版」是兩條路徑、兩個資料來源。**舊文把「盯 /api/version git_sha」直接接在合併之後,照做會等到一個永遠不會變的值。**
    - **改 Go → fresh build 驗證即可；binary 永不 commit**。CI 會編譯 source。⚠️ **唯一例外：TCC 身分錨點 `dist/officraft/officraft`**（owner 明確核可，T-5831 / 卡 `rc-2e5e4e616d1d`）——它是 launchd 的 responsible process，TCC 用 bytes 認身分，所以那份 bytes 就是要被審的東西。`.gitignore` 只放行 `dist/officraft/` 底下四個路徑；`bin/check-officraft-dist`（CI step 3）比對它的原始碼雜湊與 binary 雜湊，重建方式見 `dist/officraft/BUILD.md`。**這是一條被核可的例外，不是可以再擴張的先例**。
    - **一份副本只准跑一輪 CI;多輪＝多份副本**(T-70c9,owner 卡 `rc-bbf6a418fc23`):`bin/ci.sh` 對**它自己那份工作副本**上鎖(`<clone>/.ci-lock`,已 gitignore)。同一份 clone 起第二輪會拿到明確的 `[ci] REFUSED — this working copy is already running CI.` 並以**非零 rc** 結束。理由:ci.sh 全程原地寫這份副本(`npm ci` 重建 `node_modules`、三個 `build-*dist` staging、以及 4b1/4b2/4b3 把五個 committed 生成檔**就地重生再跟自己的備份逐位元組比對**),兩輪交錯時**失效方向不固定**——可能假紅,也可能**在一棵沒驗過的樹上變綠**,而後者就是偽造 `[ci] all green` 這個 land 權威。要同時多跑幾輪就**另開一份 clone**;鎖綁副本不綁機器,所以**跨 clone 並跑仍然可行**,那是唯一支援的並行方式。**沒有略過開關,也不要加**。⚠️ **明確不做 worktree-safe**(讓同一份 clone 真的能並跑)——owner 排除。死鎖自救、殘留與人工救援見 `docs/dev/README.md`;可執行形式 `bin/lib/ci-lock.sh`,守衛 `bin/tests/ci-lock-guard.sh`。
    - **收自己起的服務只准用自己的 PID;禁止按程式名批次殺**(T-cc3e,owner 卡 `rc-3ca48ab59773` 選①):要驗畫面/行為而在本機起一份隔離站是對的做法(自己的埠、自己的 DSN、拋棄式目錄),**但收尾時 `pkill -f "ocserverd serve"` 這類「照名字比對、一次關掉全部」的動作會連正式站一起關掉**——正式站跑的是同一支 binary、同一組參數,**名字不是身分**。實際發生過:座艙與全體 agent 斷線約兩分鐘,而現場的樣子是「服務忽然沒了」,幾乎無法回推到某個 agent 正在收自己的暫存。紀律:**起隔離站當下就記下它的 PID(或它佔的埠),收尾只關那一個**(`kill "$PID"`,或 `lsof -ti :<port> | xargs kill`);任何用名字/模式批次殺行程的收尾動作,**預設當成會誤傷正式環境**。這條的形狀比「小心一點」重要——正解是**靠構造隔離**,不是靠記得小心。(誤關後正式站的復原:先問 launchd 那台**實際**叫什麼——`launchctl list | grep officraft`——再 `launchctl kickstart -k gui/$(id -u)/<那個 label>`。**不要寫死 `com.officraft.serve`**:namespaced 安裝的 label 是 `com.officraft.serve.<ns>`(`bin/install.sh` 的 `OC_LAUNCHD_LABEL`),寫死的那條會安靜地什麼都沒打中——而你正處在「以為已經救回來了」最危險的那一刻。)
    - **§7 push 前審完整 manifest**:逐檔必要性,別把第一道審查讓給 gate。
    - ⚠️ **這個 repo 有四個 Go module,不是三個**:`server/ocserverd`、`cli/ocagent`、`cli/ocwarden`、`cli/officraft`(TCC 身分錨點,有自己的 `main_test.go`)。它們**不能互相 import**,所以 `go test ./...` 要在每個 module 各跑一次——少跑一個不會有任何東西提醒你。**乾淨 worktree 另需先 `bash bin/build-seedsdist && bash bin/build-docsdist`** 才跑得動 ocserverd 的測試(實測沒 staging 是 132 條紅,且名字全看不出跟 embed 有關;`build-bindist` 不是測試前置),細節見 `server/CLAUDE.md`。
    - 🔴 **別在文件裡列舉「有哪些閘 / 哪些跑在哪」**——列舉會過期,而且**沒有東西會叫它**:一道守衛的覆蓋面可以靜默縮小(有人把某一項搬走、某支腳本不再被派出、某個 job 的旗標改了),而那份清單照樣看起來很有道理。要寫的是「**這道閘的覆蓋面由什麼釘住**」,而那必須是**一條可執行的查詢**(「跑什麼以那支腳本的步驟標頭為準」「成員資格由檔名後綴宣告」)**或一條會紅的斷言**(下限、集合相等、差集為空)。同理:能做成守衛的就不要做成一句提醒——提醒不會在它失真的那天變色。⚠️ 例外只有一種,而且要自己講明白:**寫死的點名清單**(如 §13 land pipeline 裡那份豁免名單)——它之所以可以,正是因為**繞過它的唯一辦法是編輯它本身**,而那個編輯就是要被逼出來的那場審查。

14. **caller 身分來自 auth、非參數;intent-per-tool**(owner 定案 2026-07-10;對齊 §1「乾淨身分、不借權」;全 spec 見 `docs/design/caller-identity-convention.md`):
    - **caller 身分永遠取自 verified token `sub`(`current_actor`),絕不當請求 / tool 參數傳**。`member_id` / `agent_id` 參數**只**當**操作對象(target)**、永不是「我是誰」。
    - **自身操作(self-ops)不帶身分參數**——server 從 token 讀 caller(如 `report_waking` / `report_stopping` / `report_stopped`)。
    - **控制別的 member** 才帶 `member_id` target,且需 **admin capability**(owner-scope OR role `assistant`,如 mira——`server/ocserverd/authz.go` 的 `admin_agent` class,路由表 `requires="admin_agent"` 宣告);非 admin agent 只能操作自己。單一 resolver + 表上宣告,別每個 handler 各判。
    - **intent-per-tool**:每個 intent 是**自己一個 MCP tool**、只帶該 intent 需要的參數,**不**做「一個肥 tool + mode/phase discriminator」。

---

## 域地圖 — repo map(AI 定位用)

top-level:
- **`server/`** — Go server daemon:`ocserverd/`(**production server**:REST + SSE + MCP + reconcile,goose migrations,SPA go:embed)。folder = module = binary 同名;`bin/ocserverd` 若存在只是 gitignored 本地 build artifact(勿與 bash installer `bin/ocserver` 混淆)。詳見 `server/CLAUDE.md`。
- **`cli/`** — Go 自更新 binary:`ocagent/`(Plane A:agent-side SSE listener)· `ocwarden/`(Plane B:per-machine warden executor)。folder = module = binary 同名。詳見 `cli/CLAUDE.md`。
- **`frontend/`** — React18 + Vite5 + TS5 SPA。seam 分層 wire→mappers→types→adapter→mock→http→hooks→component。詳見 `frontend/CLAUDE.md`。
- **`conformance/`** — 語言無關黑箱套件(HTTP-only):wire 行為的回歸權威,**由 `bin/ci.sh` step (5/5) 每次全跑**(隔離 ocserverd)。條數會隨覆蓋成長,所以刻意不寫死在文件裡——這裡與 `conformance/CLAUDE.md` 兩份拷貝都曾停在 `743` 這個過期數字。詳見 `conformance/CLAUDE.md`。
- **`e2e_test/`** — Playwright 端到端(隔離 port,**絕不碰 prod**)。詳見 `e2e_test/CLAUDE.md`。
- **其他**:`spec/`(凍結 wire 契約 SSOT)· `seeds/`(語言中立 seed .md 資產,ocserverd runtime 磁碟優先直讀、單檔部署走 go:embed fallback)· `bin/`(`ci.sh` gate + 部署 / 安裝腳本)· `docs/`(架構文件)· `CLAUDE.md`(本檔,repo-wide 憲章 + 約定)· `.githooks/`(pre-commit 鏡像 CI gate)。

歷史:原 Python backend(FastAPI)已於 2026-07 退役移除;**永久回滾錨點 = git tag `py-final`**(最後一個含完整 Python 實作的 commit)。

########## RED PROOF D — remove the badge's 1px page-colour ring (the thing that JUSTIFIES 2.74) ##########
0

[token-roles] 1 violation(s) — a T-081b token split has been undone.
These re-merges look FINE in the built-in dark theme and break only on light themes,
which is why they are pinned here rather than left to review:

  styles/theme.css:0  .nav-tab__badge { outline: (missing)
      .nav-tab__badge has no outline declaration — without the page-colour ring the pill is measured against the wrong background (--color-indigo on an active tab is 2.74:1).

exit=0
restored exit=0

# T-081b 版面截圖(補件)

先前寬版驗收只留了像素量測、沒留畫面,這裡把畫面補上。

## 怎麼拍的

- 起站:`cd frontend && npm run dev -- --port 5199`。前端預設走 mock adapter
  (`frontend/src/api/index.ts`,`VITE_USE_MOCK` 未設 = mock),`AuthGate` 在 mock 模式
  **不會**出現登入牆、直接進辦公室,所以不需要登入步驟。
- 驅動腳本:`shoot.mjs`(和 `canvas-sides.mjs` / `zonecheck.mjs` 同一套做法 —— 從
  `frontend/package.json` 取 playwright)。`node shoot.mjs builtin|canvas|pack`。
- 每張都停在**監控頁**:這一頁有帳號卡、機器表格、AI 會話表格,內容欄一變寬,表格就跟著
  攤開,寬窄差異一眼可見(辦公室頁中間大多是空聊天區,看不出來)。
- 數字全部是實測:外區 =(視窗寬 − `.app__main` 實際寬)/ 2,內容欄 = `.app__main` 實際寬,
  橫向捲動 = `document.scrollingElement.scrollWidth > clientWidth`。原始輸出在
  `measurements.jsonl`。

## ⚠️ 一個和任務描述不符的事實:寬版沒有外區

任務要「寬版看得出左右外區與內容欄的關係」「確認外區在不同寬度下維持固定寬度」。
**實際行為是:寬版的外區固定為 0px**,內容欄吃滿整個視窗。

這不是 bug,是 owner 2026-07-27 當天改回去的決定,程式碼裡有原註記
(`frontend/src/components/chrome.css` 第 23–33 行):

> 本票一度依 owner 2026-07-27 的「寬版左右兩邊還是要有一點外區」改成每側保留 48px,
> 同日他看過實際效果後改回 ——「寬版好像真的看不出什麼,不然寬版就不要留白好了」。
> ⇒ 最外層畫布(含主題背景圖)成為**窄版限定**的一層,寬版看不到它。

所以下面寬版三張(1440 / 1280 / 1040)的外區都是 0px —— 「外區在不同寬度下維持固定寬度」
這個檢查在寬版不成立(它固定是 0)。真正會出現外區的是**窄版且視窗寬 > 1040px**。

---

## 逐張說明

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 |
| --- | --- | --- | --- | --- |
| `01-wide-1440-office.png` | 寬版 1440,內建「辦公室」主題,監控頁。表格攤滿整個視窗。 | **0px** | 1440px | 無 |
| `02-narrow-1440-office.png` | 窄版 1440(預設版面),同一畫面對照。左右各留 200px 外區,內容欄封頂在 1040。 | **200px** | 1040px | 無 |
| `03-wide-1280-office.png` | 寬版 1280。 | **0px** | 1280px | 無 |
| `04-wide-1040-office.png` | 寬版 1040。 | **0px** | 1040px | 無 |
| `05-narrow-480-office.png` | 窄版 480(手機寬度)。外區歸零,頁籤列與表格自行收窄。 | **0px** | 480px | 無 |
| `06-narrow-375-office.png` | 窄版 375(手機寬度)。外區歸零。 | **0px** | 375px | 無 |

第 1、2 張是這次補件的重點:**同一畫面、同一主題、同一視窗寬,只差寬/窄版**,
對照起來就是「寬版把 200px 外區還給內容欄」。

---

## 外區背景圖(`backgrounds.canvas`)

手邊沒有現成含背景圖的主題包(小精靈村那份也沒有),所以自造了一張圖:160×160 的
深綠底 + 斜線 + 一個圓點,用 canvas 畫出來的純幾何圖形,**沒有用到任何真實素材**。
data-URI 5,238 bytes(遠低於 64 KiB 上限)。包成兩份測試主題包,已存檔:
`../shots-pack/canvas-cover.theme.json`、`../shots-pack/canvas-sides.theme.json`。
兩份都**匯入無警告**。

為什麼要兩份:`backgroundModes.canvas` 是三選一(tile / sides / cover),
而 **tile 和 sides 只畫在內容欄旁邊的外區** —— 寬版外區 0px、手機也是 0px,
兩種都看不到圖(產品自己的說明文字就是這樣寫的,見 `i18n/locales/en.ts` 的
`themeBackgroundHint`)。所以:

- 想在**寬版 1440 與手機 375 都看得到圖** → 只能用 `cover`(填滿整個視窗),
  外加把三個分區色調成半透明,圖才會從座艙底下透出來 —— 這正是 T-081b 分區
  token 存在的理由。第 07、08 張就是這個組合。
- 要看**外區真的吃得到圖**,得用 `sides` 且**窄版 + 視窗 > 1040px**。第 09 張補這個。

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 |
| --- | --- | --- | --- | --- |
| `07-canvas-bg-wide-1440-cover.png` | 自造背景圖 · `cover` 鋪法 · 寬版 1440。圖縮放填滿整個視窗、置中,三個分區半透明,整個座艙浮在圖上。 | **0px** | 1440px | 無 |
| `08-canvas-bg-narrow-375-cover.png` | 同一份包 · 窄版 375(手機)。圖照樣鋪滿,表格自動收成堆疊卡片,沒有破版。 | **0px** | 375px | 無 |
| `09-canvas-bg-narrow-1440-sides.png` | `sides` 鋪法 · 窄版 1440。左右外區底部各**一份**圖(不重複、釘在視窗底),內容欄不被侵入。 | **200px** | 1040px | 無 |

補一句:375 那兩張裡頁籤列(辦公室/請示/…/使用說明)自己會橫向捲,那是頁籤列
**自己的**捲動容器,不是頁面捲動 —— 量到的
`document.scrollingElement.scrollWidth == clientWidth`,文件層沒有橫向捲動。

---

## 小精靈村主題包(本次改動之前做的包)

匯入結果:**成功匯入,不是被拒**。畫面上出現一則警告:

> 已匯入,但有 1 個用詞代碼不認得、已略過:`profile.themeOffice`

被略過的就是這 1 個 —— `profile.themeOffice`(包裡想把「辦公室」主題名改成「精靈村」;
主題身分用詞已經改由不可覆寫的 `themeIdentity` / `themeMarkers` 提供,所以這個代碼不再存在)。
包裡的 61 個顏色 token 全部認得,沒有任何一個被擋。原始文字另存
`smurf-import-warning.txt`。

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 | 匯入警告 |
| --- | --- | --- | --- | --- | --- |
| `10-smurf-import-warning.png` | 設定 › 主題,匯入完成當下 —— 看得到那則「已略過」警告,以及清單裡多出「精靈村(自訂)」一列。 | 200px | 1040px | 無 | 已匯入,但有 1 個用詞代碼不認得、已略過:`profile.themeOffice` |
| `11-smurf-wide-1440.png` | 小精靈村主題包 · 寬版 1440 · 監控(包裡叫「瞭望台」)。 | **0px** | 1440px | 無 | 同上 |
| `12-smurf-narrow-1440.png` | 小精靈村主題包 · 窄版 1440,同一畫面對照。 | **200px** | 1040px | 無 | 同上 |
| `13-smurf-narrow-480.png` | 小精靈村主題包 · 窄版 480。 | **0px** | 480px | 無 | 同上 |
| `14-smurf-narrow-375.png` | 小精靈村主題包 · 窄版 375(手機)。 | **0px** | 375px | 無 | 同上 |

包的用詞(村莊/差事/瞭望台)、頭像、nav 圖示、logo、整組淺色調都吃進去了,
版面行為與內建主題完全一致。

順帶一提:那則警告雖然文案是「已匯入,但…」,但用的是紅框樣式(`.set-warn`),
乍看像匯入失敗。不影響功能,但 owner 若覺得容易誤讀,可以另開一張調樣式。

---

## 小精靈村**最終包**(完整套用新架構)

`../shots-pack/smurf-village.theme.json` 已被主題包同事覆蓋成最終版。
**這一組(15~18)拍的是最終包;上面 11~14 那組是被覆蓋掉的舊版,保留作對照。**

兩版差在哪:

| | 舊包(11~14) | 最終包(15~18) |
| --- | --- | --- |
| 顏色槽 | 61 | **72**(補齊新拆出的槽,含三層分區底色) |
| 用語 | 186 條,其中 1 條不認得 | **185 條,全部認得** |
| 匯入警告 | 「已匯入,但有 1 個用詞代碼不認得、已略過:`profile.themeOffice`」 | **零警告**(那條已移除) |
| `backgrounds.canvas` | 無 | **有**,`backgroundModes.canvas` = **`cover`** |
| 三層分區底色 | 未設(跟著 `--color-bg`) | 各自設值,alpha **0.8** |

匯入驗證(走產品自己的 設定 › 主題 › 匯入):**零警告、零錯誤**,畫面上完全沒有出現
警告框 —— 見 `smurf-final-import.png`(對照舊包的 `10-smurf-import-warning.png` 有紅框)。
腳本裡也把這件事寫成硬條件:`shoot.mjs` 的 `pack2` group 一旦收到任何警告就直接 throw、
不會產圖。原始文字另存 `smurf-final-import-warning.txt`(內容為「(無警告)」)。

背景圖是 **`cover` 整頁鋪滿**,不是外區鋪法,所以**寬版、窄版、手機都看得到**
(這正是外區鋪法 tile/sides 做不到的 —— 見上一節)。三層底色保留 **80% 不透明度**,
所以文字底下是平的、可讀性不受影響,圖只從**邊緣與元件間隙**透出來。

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 | 匯入警告 |
| --- | --- | --- | --- | --- | --- |
| `15-smurf-final-wide-1440.png` | 最終包 · 寬版 1440 · 瞭望台(監控)。內容欄吃滿視窗,背景圖從頂列上緣、卡片之間與頁面下半的空白處透出來。 | **0px** | 1440px | 無 | 無 |
| `16-smurf-final-narrow-1440.png` | 最終包 · 窄版 1440,同一畫面對照。左右各 200px 外區,這是**村莊畫面看得最清楚**的一張。 | **200px** | 1040px | 無 | 無 |
| `17-smurf-final-narrow-480.png` | 最終包 · 窄版 480。 | **0px** | 480px | 無 | 無 |
| `18-smurf-final-narrow-375.png` | 最終包 · 窄版 375(手機)。 | **0px** | 375px | 無 | 無 |

⚠️ 對 17、18 兩張要如實說:手機寬度下版面沒有破,也確實有底圖,**但因為內容欄吃滿整個
視窗、80% 不透明的分區底色蓋住幾乎全部畫面,圖實際只看得出「一層淡淡的綠底」,
看不出是村莊**。要真正看到村莊,得是窄版且視窗夠寬(第 16 張)。

---

## 拿掉每列重複標籤之後(commit `650754c`)

`650754c`「分區之後,每列再掛一顆同義標籤是重複的;而那顆標籤正是可偽造的那一環」
把主題清單每列右邊那顆「內建」/「自訂」小標籤拿掉了,分組資訊只留在**群組標題**上。
這兩張就是改完的樣子。

拍攝條件刻意和舊圖對齊:先匯入 `../shots-pack/smurf-village.theme.json`(最終包,零警告)
讓清單同時有內建與自訂兩列,**匯入後不切換主題**、維持內建深色,視窗 1440 窄版 ——
和 `smurf-final-import.png` 完全同條件,可以直接前後對照。

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 | 匯入警告 |
| --- | --- | --- | --- | --- | --- |
| `19-theme-list-no-row-tags.png` | 設定 › 主題 清單,內建主題,窄版 1440。每列右邊的「內建」/「自訂」小標籤**已消失**,只剩「內建」「自訂」兩個群組標題;右側的下載/編輯/刪除圖示鈕維持原樣、兩列右緣仍對齊。 | 200px | 1040px | 無 | 無 |
| `20-theme-select-optgroups.png` | 頂列右上使用者選單 › 偏好設定 › 主題下拉(展開)。看得到 `<optgroup>` 分成「內建 → 辦公室」與「自訂 → 精靈村」兩組。 | 200px | 1040px | 無 | 無 |

**對照的舊圖**:`smurf-final-import.png`(同一個畫面、同一寬度、同樣是匯入完成後的清單)
—— 那張每列名字後面還跟著一顆「內建」/「自訂」小標籤,19 這張沒有了。
更早的 `10-smurf-import-warning.png` 也看得到那顆舊標籤(那張還多一個紅色警告框,
因為用的是舊包)。

⚠️ 第 20 張有一個拍攝手法要講清楚:這個主題下拉是**原生 `<select>`**,
原生下拉的彈出層由作業系統繪製、不會進到頁面截圖裡。所以拍之前用 JS 把同一顆 select 的
`size` 設成選項數、並放開它的固定高度,讓它就地攤成清單框。
**DOM 和 `<optgroup>` 都是產品自己渲染的那一份,沒有另外造假**,只是強制展開。
腳本同時把讀到的分組結構印出來存進 `measurements.jsonl`:
`[{"group":"內建","options":["辦公室"]},{"group":"自訂","options":["精靈村"]}]`。

---

## 偏好設定下拉:正常樣子 + 真正的原生彈出層

owner 看過第 20 張「強制攤開」的版本後想看下拉本身,所以補這兩張。
兩張都先匯入最終包(零警告)讓下拉裡有兩組可選,**不切換主題**、維持內建深色。

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 |
| --- | --- | --- | --- | --- |
| `21-preferences-panel.png` | 偏好設定面板**正常狀態**:頂列右上使用者選單展開 → 偏好設定,主題下拉維持未展開的原始外觀(collapsed,顯示目前值「辦公室」),連同「語言(中文/English)」「版面(窄版/寬版)」一起入鏡。窄版 1440,背後停在監控頁。 | 200px | 1040px | 無 |
| `22-theme-select-native-popup.png` | **真正點開時的原生彈出層** —— 由 macOS 畫的 select popup,看得到「內建 → ✓ 辦公室(藍色選取)」與「自訂 → 精靈村」兩組。 | — | — | — |

第 21 張是純網頁截圖(Playwright),`select.size` 讀回來是 `0` = 沒有被動過手腳,是產品原本的樣子。

### 第 22 張是怎麼拍到的(以及為什麼前面說拍不到)

原生 `<select>` 的彈出層由 WindowServer 繪製、不在網頁畫面裡,所以
`page.screenshot()` 永遠拍不到它 —— 這就是第 20 張當初改用「強制攤開」的原因。
這次改走 OS 螢幕截圖,**拍到了**:

- **headed 瀏覽器**(`chromium.launch({ headless: false })`)開在固定位置,
  用 macOS 的 **`screencapture`** 抓,腳本是 `native-popup.mjs`。
- 踩到的一個坑:這個 shell 位在 launchd 的 **Background session**,直接跑
  `screencapture` 只會得到 `could not create image from display`;要用
  **`launchctl asuser <uid>`** 丟進 Aqua session 才畫得出來。
- **沒有跳出任何權限視窗** —— 螢幕錄製權限本來就已經授權過,一次就成功。
- 截的範圍是 **`-R` 指定的瀏覽器視窗那一塊**(`window.screenX/Y/outerWidth/outerHeight`
  算出來的 `40,65,1200,860`),**不是整個桌面** ——桌面上有使用者的私人內容,
  不該進到要拿給別人看的截圖裡。
- 原生彈出層是 modal 的:Playwright 的 `click()` 會一直等到它關掉才 resolve,
  所以腳本**刻意不 await** 那個 click,趁彈出層開著的當下才截。
- 附一張同視窗、同一條 screencapture 路徑但**未展開**的對照
  `native-popup-before.png`,證明 22 那張的差別真的是彈出層本身。

⚠️ 一句話總結給 owner:第 20 張是「把同一顆 select 強制攤開」的示意(DOM 真、
但不是他點下去會看到的樣子),**第 22 張才是他真正點開時長的樣子**。

---

## 拿掉 `<optgroup>` 分區之後(round 7)

owner 2026-07-27 收斂後的決定:**偏好設定的下拉維持原生 `<select>`,但不用分區**
——「原本的下拉式選單我覺得就很好了」「就算真的沒有顯示內建或自訂也沒關係,
只要**設定**有標示出來就好」。所以:

- 快速選單的下拉 → **平面清單**,沒有 `<optgroup>`、沒有任何「內建/自訂」字樣;
  **排序固定內建在前、自訂在後**(內建那顆寫死在 JSX 最前面,主題包的任何欄位
  ——名稱、匯入順序——都碰不到歸屬與排序)。
- 設定 › 主題 的分組標示(`role="group"` + `.ts-group-head`,顏色吃不可覆寫的
  `--color-marker-*`)**維持現狀不動**,那是他要保留的標示位置(第 19 張)。

拍攝條件和第 21 張刻意完全一致(先匯入 `../shots-pack/smurf-village.theme.json`
最終包、**匯入後不切換主題**、窄版 1440),可以直接前後對照。

| 檔名 | 這是什麼畫面 | 外區(每側) | 內容欄 | 橫向捲動 | 下拉結構 |
| --- | --- | --- | --- | --- | --- |
| `23-preferences-panel-flat-select.png` | 偏好設定面板**收合態**,主題 / 語言 / 版面三區。主題下拉顯示目前的「辦公室」。 | 200px | 1040px | 無 | `optgroup` **0 個**;option 順序 = `辦公室`(內建)→ `精靈村`(自訂) |

腳本:`node shoot.mjs flat`。腳本裡把兩件事寫成硬條件,不符就直接 throw、不會產圖:
`optgroupCount === 0`、`optionOrder[0].value === "office"`。實測值一併寫進
`measurements.jsonl`。

**展開態不補拍**:原生下拉的彈出層由作業系統繪製,已有第 22 張(OS 螢幕截圖)。

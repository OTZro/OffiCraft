{
  "tools": [
    {
      "description": "The station's own build identity: which version and git sha it is RUNNING right now. Settle \"has my change shipped?\" against git_sha (an ancestry test), never the version string - a self-build honestly reports 0.0.0. Distinct from check_release, which asks GitHub whether a NEWER release exists.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "get_version"
    },
    {
      "description": "Read the org-adjustable settings (owner/admin agent).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "get_settings"
    },
    {
      "description": "Edit settings (owner-login and agent token TTLs / handover threshold); live immediately.",
      "inputSchema": {
        "$defs": {
          "ThemeBundleDTO": {
            "description": "One owner-authored theme colour bundle (T-16a1 P2). `id` is a client-generated stable slug (`^[a-z0-9][a-z0-9-]{1,63}$`), unique within the owner's set and never a built-in name (`office` / `xian`). `name` is the display label (trimmed, 1..80 runes). `colors` maps `--color-*` token names \u2014 each MUST be a token defined in styles/theme.css \u2014 to CONCRETE colour values (hex / rgb() / rgba() / hsl() / hsla() / transparent only; no var(), no color-mix(), no arbitrary CSS). 1..200 pairs. The server 422s any bundle that violates the shape, the token whitelist, or the colour grammar. `wording` (optional, T-16a1 P3) carries per-language message-key text overrides; see its own description. `fonts` (optional, T-16a1 P4) carries font-family choices; see its own description.",
            "properties": {
              "id": {
                "title": "Id",
                "type": "string"
              },
              "name": {
                "title": "Name",
                "type": "string"
              },
              "colors": {
                "additionalProperties": {
                  "type": "string"
                },
                "title": "Colors",
                "type": "object"
              },
              "fonts": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Optional font-family choices (T-16a1 P4). Maps a `--font-*` token name (`--font-sans` / `--font-title` only) to a font-family stack string. The value is NOT an arbitrary string: it must be one of a small CLOSED allowlist of safe, built-in font-family stacks (themeFonts.source.json \u2014 the same set the theme editor's font dropdown offers); no url(), no @font-face, no external font, no CSS structure characters. Absent = the theme keeps the default fonts. The server 422s any fonts that violates the token set or the safe-family allowlist.",
                "title": "Fonts",
                "type": "object"
              },
              "wording": {
                "additionalProperties": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Wording Lang",
                  "type": "object"
                },
                "description": "Optional per-language wording overrides (T-16a1 P3). Outer key = UI language (`zh` / `en` only); inner map = an i18n message-key (an internal code path such as `nav.tasks`, drawn from the generated messageKeys whitelist) -> the owner's replacement PLAIN TEXT. Values are plain text only (trimmed, <=200 runes, control chars / newlines rejected, per-language entry count capped); no HTML/CSS. Absent = no overrides. The server 422s any wording that violates the language set, the key whitelist, or the value rules.",
                "title": "Wording",
                "type": "object"
              },
              "avatars": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Optional per-role avatar images (T-16a1 P5; extended per role in T-ea81). Keys are the closed set `member` (\u4e00\u822c\u6b63\u8077 member) / `outsource` (\u5916\u5305 outsource worker) / `owner` (the human CEO / owner) / `assistant` (a member whose role is `assistant`, e.g. Mira). Each value is an EMBEDDED image encoded as a base64 `data:` URI so the image travels inside the bundle on export/import. The value is NOT arbitrary: only a `data:image/<mime>;base64,<...>` URI whose mime is a whitelisted RASTER format (`image/png` / `image/jpeg` / `image/webp`) is accepted. SVG (`image/svg+xml`) is REJECTED (it can carry script/onload \u2014 XSS). The base64 must decode, the decoded byte size is capped (<=64 KiB) and the string length capped, and the leading magic bytes must match the declared mime (PNG `89 50 4E 47`, JPEG `FF D8 FF`, WEBP `RIFF....WEBP`) \u2014 a value that declares one mime but carries another is rejected. Absent = that role falls back to the built-in avatar glyph (office never degrades). The server 422s any avatars that violates the key set, the mime whitelist, the size caps, the base64, or the magic-byte check.",
                "title": "Avatars",
                "type": "object"
              },
              "logo": {
                "description": "Optional studio logo image (T-ea81). A single EMBEDDED image encoded as a base64 `data:` URI that replaces the built-in logo mark shown in the top bar. Same strict image gate as `avatars`: only a `data:image/<mime>;base64,<...>` URI whose mime is a whitelisted RASTER format (`image/png` / `image/jpeg` / `image/webp`) is accepted; SVG (`image/svg+xml`) is REJECTED (script/onload XSS); the base64 must decode, the decoded byte size is capped (<=64 KiB) and the string length capped, and the leading magic bytes must match the declared mime (PNG `89 50 4E 47`, JPEG `FF D8 FF`, WEBP `RIFF....WEBP`). Absent = the built-in logo mark is used (office never degrades). The server 422s any logo that violates the mime whitelist, the size caps, the base64, or the magic-byte check.",
                "title": "Logo",
                "type": "string"
              },
              "navIcons": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Optional per-tab navigation icon images (T-ea81). Keys are the closed set of the five nav tabs `office` (\u8fa6\u516c\u5ba4) / `replies` (\u8acb\u793a) / `tasks` (\u4efb\u52d9) / `monitor` (\u76e3\u63a7) / `guide` (\u4f7f\u7528\u8aaa\u660e); each value is an EMBEDDED image encoded as a base64 `data:` URI that replaces that tab's built-in icon. Same strict image gate as `avatars`: only a `data:image/<mime>;base64,<...>` URI whose mime is a whitelisted RASTER format (`image/png` / `image/jpeg` / `image/webp`) is accepted; SVG (`image/svg+xml`) is REJECTED (script/onload XSS); the base64 must decode, the decoded byte size is capped (<=64 KiB) and the string length capped, and the leading magic bytes must match the declared mime (PNG `89 50 4E 47`, JPEG `FF D8 FF`, WEBP `RIFF....WEBP`). Absent = that tab falls back to its built-in icon (office never degrades). The server 422s any navIcons that violates the key set, the mime whitelist, the size caps, the base64, or the magic-byte check.",
                "title": "Nav Icons",
                "type": "object"
              }
            },
            "required": [
              "id",
              "name",
              "colors"
            ],
            "title": "ThemeBundleDTO",
            "additionalProperties": false,
            "type": "object"
          }
        },
        "properties": {
          "codex_compaction_threshold": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Codex context-compaction threshold, 1 through 10.",
            "title": "Codex Compaction Threshold"
          },
          "doc_cap_chars_boot_sequence": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on a 啟動程序 block of the boot context, in CHARACTERS (Unicode code points). ONE knob for BOTH runtimes (claude and codex), each document measured on its own text. Must be at least this document's shipped default and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars Boot Sequence"
          },
          "doc_cap_chars_duty": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on a role's DUTY doc (the role definition), in CHARACTERS (Unicode code points). Must be at least this segment's own shipped default — which is deliberately much smaller than the other three segments' — and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars Duty"
          },
          "doc_cap_chars_insight": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on a role's INSIGHT doc, in CHARACTERS (Unicode code points). Must be at least this segment's shipped default and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars Insight"
          },
          "doc_cap_chars_learning": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on a role's LEARNING doc (the lessons doc), in CHARACTERS (Unicode code points). Must be at least this segment's shipped default and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars Learning"
          },
          "doc_cap_chars_manual_learnings": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on a TASK MANUAL's learnings doc, in CHARACTERS (Unicode code points). Must be at least this segment's shipped default and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars Manual Learnings"
          },
          "doc_cap_chars_manual_sop": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on a TASK MANUAL's sop_md doc, in CHARACTERS (Unicode code points). Must be at least this segment's shipped default and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars Manual Sop"
          },
          "doc_cap_chars_system_interaction": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The size cap on the 系統互動 block of the boot context, in CHARACTERS (Unicode code points). Must be at least this document's shipped default and at most 100000. Read the current values from get_settings rather than assuming a number.",
            "title": "Doc Cap Chars System Interaction"
          },
          "custom_themes": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/ThemeBundleDTO"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Replace the owner's custom theme bundles (T-16a1 P2) with this array (each `{id,name,colors}`). Omit to leave them unchanged; `[]` clears them. Every bundle is validated against the shape, the theme.css token whitelist, and the concrete-colour grammar \u2014 any violation is a 422 and nothing is written. When this and display_theme are patched together, display_theme is validated against the POST-patch set; and deleting the active custom theme resets display_theme to \"\".",
            "title": "Custom Themes"
          },
          "display_language": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The owner's cockpit language (T-0b41-p2) \u2014 trimmed; \"\" clears it back to unset. Must be one of zh, en (or \"\"); anything else is a 422.",
            "title": "Display Language"
          },
          "display_theme": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The owner's cockpit visual theme (T-0b41-p2) \u2014 trimmed; \"\" clears it back to unset. Must be one of office, xian (or \"\"); anything else is a 422.",
            "title": "Display Theme"
          },
          "display_wide": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "Turn the WIDE cockpit layout on/off (T-756f) \u2014 true lifts the centred ~1040px content column (the side gutters stay), false restores it. A plain boolean with no unset state: omit the field to leave it unchanged.",
            "title": "Display Wide"
          },
          "handover_pct": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Handover Pct"
          },
          "monitoring_refresh_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Minimum interval between monitoring and machine refreshes, in seconds (1 through 60).",
            "title": "Monitoring Refresh Seconds"
          },
          "org_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The studio display name (T-d693) \u2014 trimmed, max 80 runes; \"\" clears it back to the localized default. A value longer than 80 runes is a 422.",
            "title": "Org Name"
          },
          "outsource_max_parallel": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Outsource Max Parallel"
          },
          "owner_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The owner's display nickname (T-0b41) \u2014 trimmed, max 80 runes; \"\" clears it back to the localized default. A value longer than 80 runes is a 422.",
            "title": "Owner Name"
          },
          "push_contact_email": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The push contact address (T-8a82) \u2014 trimmed, max 254 runes; \"\" clears it back to unset and stops all Web Push delivery. A value must be a single `local@domain` address whose domain is a real public one: a malformed address, or one on a reserved suffix (.local, .localhost, .internal, .test, .invalid, .example), is a 422 \u2014 those are exactly the values the push gateways reject with BadJwtToken, which would take push down silently.",
            "title": "Push Contact Email"
          },
          "agent_token_ttl": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Token Ttl"
          },
          "owner_token_ttl": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner Token Ttl"
          },
          "updater_auto_update": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Updater Auto Update"
          },
          "updater_receive_beta": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Updater Receive Beta"
          }
        },
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_settings"
    },
    {
      "description": "Check GitHub Releases for a newer official OffiCraft version.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "check_release"
    },
    {
      "description": "Trigger a software upgrade to the latest GitHub release.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "upgrade_station"
    },
    {
      "description": "List all members, including outsource members by default. fields=light preserves kind for every row (presence-derived MemberDTO[]).",
      "inputSchema": {
        "properties": {
          "fields": {
            "description": "The literal STRING \"light\" (not a boolean, and no other value does anything) asks for the identity-only projection of every row: id, member_no, name, kind, role_key/role_name, runtime, roster_status, avatar. It DROPS the per-member unread chat count and the observed live-host state, which is the expensive half of this endpoint. Pass it whenever you are looking someone up or resolving a name to an id; omit it only when you actually need unread counts or where a member is running.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_members"
    },
    {
      "description": "Hire a member (server mints the id). runtime is claude/codex (absent = claude); model is provider-specific. Pure seam, no UI (\u00a79.1).",
      "inputSchema": {
        "properties": {
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Effort"
          },
          "kind": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Kind"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Model"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "role_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Role Key"
          },
          "runtime": {
            "anyOf": [
              {
                "enum": [
                  "claude",
                  "codex"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Runtime"
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "hire_member"
    },
    {
      "description": "Read one roster member (removed \u2192 404).",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "type": "object"
      },
      "name": "get_member"
    },
    {
      "description": "Edit a member (name / runtime / model / effort). runtime is claude/codex; blank name / bad runtime / bad effort \u2192 422.",
      "inputSchema": {
        "properties": {
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Effort"
          },
          "member_id": {
            "type": "string"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Model"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Name"
          },
          "runtime": {
            "anyOf": [
              {
                "enum": [
                  "claude",
                  "codex"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Runtime"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_member"
    },
    {
      "description": "Activate: write desired_state=online intent (does NOT flip online).",
      "inputSchema": {
        "properties": {
          "machine_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Machine Id"
          },
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "activate_member"
    },
    {
      "description": "Relocate a member to a machine (placement only; never touches desired_state). Also accepts an outsource-worker id: the same move-one-agent verb relocates the worker. machine_id is REQUIRED (owner 2026-07-27): a relocate NAMES the destination machine and no longer doubles as an unpin \u2014 an absent key is a 422, an explicit null or \"\" is a 400.",
      "inputSchema": {
        "properties": {
          "machine_id": {
            "title": "Machine Id",
            "type": "string"
          },
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id",
          "machine_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "relocate_member"
    },
    {
      "description": "Deactivate: desired_state=offline + stamp stopping_since (retains row).",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "deactivate_member"
    },
    {
      "description": "Force-stop: robust STOP now, bypassing the graceful-stop grace.",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "force_stop_member"
    },
    {
      "description": "report_waking(): stamp the caller's waking + clear recycle markers.",
      "inputSchema": {
        "properties": {
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Model"
          }
        },
        "additionalProperties": false,
        "type": "object"
      },
      "name": "report_waking"
    },
    {
      "description": "report_stopping(): stamp the caller's stopping_since (graceful stop).",
      "inputSchema": {
        "properties": {},
        "additionalProperties": false,
        "type": "object"
      },
      "name": "report_stopping"
    },
    {
      "description": "report_stopped(): anchor the caller's stopped; fire recycle kill.",
      "inputSchema": {
        "properties": {},
        "additionalProperties": false,
        "type": "object"
      },
      "name": "report_stopped"
    },
    {
      "description": "restart_self(): self-triggered recycle (online-only 409; min-liveness 429).",
      "inputSchema": {
        "properties": {
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Reason"
          }
        },
        "additionalProperties": false,
        "type": "object"
      },
      "name": "restart_self"
    },
    {
      "description": "Refocus a member's context (online-only, else 409).",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "refocus_member"
    },
    {
      "description": "Dismiss a member (soft delete). Pure seam, no UI (\u00a79.1).",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "dismiss_member"
    },
    {
      "description": "Last 5 raw /in requests of one webhook endpoint (debug).",
      "inputSchema": {
        "properties": {
          "endpoint_id": {
            "type": "string"
          },
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id",
          "endpoint_id"
        ],
        "type": "object"
      },
      "name": "list_webhook_requests"
    },
    {
      "description": "The SAME bounded wake snapshot as resume_summary, for a TARGET member (member_id) instead of the caller — control-others, admin_agent+ only (owner-scope or role=assistant); an ordinary agent gets 403. Same identity/chat/light-task-rows/roster/machines/overview/note shape, assembled by the identical resumeSnapshotParts function (so the roster and machine blocks cannot drift from what that member would get on waking; note that machines.you_are_on resolves for the TARGET member, not for you); resume_summary itself is unchanged and still identity-locked to the caller.",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "get_member_resume_summary"
    },
    {
      "description": "Post a chat message (sender = verified JWT sub; auto SSE fan-out). ``to`` must name the owner or an active AI member; unknown, removed, and machine ids are rejected. Presence is not a gate: an offline member keeps its durable mailbox.",
      "inputSchema": {
        "$defs": {
          "ChatAttachmentInputDTO": {
            "description": "An attachment carried on a posted chat message \u2014 either INLINE BYTES or a\nREFERENCE to a blob already in the shared attachment store.\n\nInline: ``data_b64`` is the bytes, base64-encoded \u2014 a data-URI\n(``data:<mime>;base64,....``) OR bare base64 (the handler strips a\n``data:...;base64,`` prefix if present). ``mime`` and ``filename`` are\noptional: an omitted ``mime`` is sniffed for images (else\n``application/octet-stream``); an omitted ``filename`` defaults for pasted\nimages.\n\nReference: ``id`` names an EXISTING blob (minted by\n``POST /api/chat/attachments`` \u2014 the streaming upload seam that keeps file\nbytes out of a JSON tool call). The stored blob's mime/filename are\nauthoritative: ``filename``/``mime`` sent alongside ``id`` are IGNORED (so\nthe upload response ``{id, mime, filename}`` can be pasted back verbatim).\nAn unknown ``id`` is a 400; carrying BOTH ``id`` and ``data_b64`` is a 400\n(ambiguous intent). An item with neither ``id`` nor ``data_b64`` is a\n400 (T-e2b2: it used to be dropped silently, so a sender that named a file it\nnever sent got a success and the recipient got nothing). The reply-card ANSWER face is\ninline-only: it decodes ``data_b64`` and has never resolved ``id`` references,\nso an item carrying only an ``id`` there is a 400 (T-e2b2 — it used to be\ndropped silently).",
            "properties": {
              "data_b64": {
                "default": "",
                "title": "Data B64",
                "type": "string"
              },
              "filename": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Filename"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Id"
              },
              "mime": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Mime"
              }
            },
            "title": "ChatAttachmentInputDTO",
            "type": "object",
            "additionalProperties": false
          }
        },
        "properties": {
          "attachments": {
            "items": {
              "$ref": "#/$defs/ChatAttachmentInputDTO"
            },
            "title": "Attachments",
            "type": "array"
          },
          "body": {
            "default": "",
            "title": "Body",
            "type": "string"
          },
          "meta": {
            "additionalProperties": true,
            "title": "Meta",
            "type": "object"
          },
          "to": {
            "title": "To",
            "type": "string"
          }
        },
        "required": [
          "to"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "post_chat"
    },
    {
      "description": "List the chat stream (?with=<id>&limit=<n>; oldest\u2192newest). History paging: before_ts + before_id (both together) return the limit messages strictly OLDER than that keyset cursor \u2014 a history page NEVER advances the read watermark. Re-read specific messages by id: ids=<id>&ids=<id> returns those messages in full without a peer and without a cursor; the ids schema states who may read what, the per-call limit, and what an unknown id does.",
      "inputSchema": {
        "properties": {
          "before_id": {
            "type": "string"
          },
          "before_ts": {
            "type": "number"
          },
          "caller_only": {
            "default": false,
            "type": "boolean"
          },
          "ids": {
            "description": "RE-READ SPECIFIC MESSAGES BY ID — repeatable (``?ids=<id>&ids=<id>``), returning those messages IN FULL (whole body, attachment refs and all), oldest→newest.\n\nThis is what makes the wake snapshot's fold marker honest (T-a828). ``ChatMessageDTO.body_omitted_chars`` > 0 says THIS message is here with part of its text folded away and tells the reader to re-read it with get_chat — but until this parameter existed get_chat took only a peer plus a paging cursor, so there was NO WAY TO NAME THE FOLDED MESSAGE. The promise beside the fold was not keepable, which made folding a silent drop.\n\nANSWERED ON ITS OWN: when ``ids`` is present, ``with``, ``limit``, ``before_ts``/``before_id`` and ``peek`` are NOT consulted, and a by-ids read NEVER advances a read watermark — re-reading a message you were already shown is not reading the conversation. Blank entries are dropped and duplicates collapse; an all-blank or empty set behaves exactly as if the parameter had not been sent.\n\nYOURS ONLY, AND ONLY HERE: an id whose ``sender`` and ``recipient`` are both someone other than the verified caller is REFUSED with 403, and ONE such id refuses the WHOLE call rather than just that entry. Holding an id is not permission to read a conversation between two other people, and no parameter widens a by-ids read past that. This bound belongs to THIS PARAMETER, not to ``get_chat`` as a whole: the ordinary listing filters on ``with`` — a PARTICIPANT — not on the caller, so a plain read of a peer's line still answers with a conversation the caller was never in (designed behaviour; ``caller_only`` is what narrows a listing to the caller). Do not read this paragraph as a claim about the endpoint.\n\nALL OR NOTHING ON AN UNKNOWN ID: an id no message carries refuses the WHOLE call with 404 and names it. Deliberately not 'skip it and return the rest': a short array is indistinguishable from the fold this parameter exists to undo, so a caller could not tell a deleted message from one it simply did not ask for.\n\nAT MOST 20 DISTINCT IDS per call (counted after blanks are dropped); more is a 400 that states the limit. The cap is the response bound: 20 × the 4,000-rune body cap is the worst case one call can emit. It is not 'unfold a whole snapshot in one call' — name the ones that matter and call again.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "limit": {
            "type": "integer"
          },
          "peek": {
            "description": "The literal STRING \"true\" (not a boolean, and any other value is ignored) reads the conversation WITHOUT advancing your read watermark, so the messages you see stay unread and the auto read-receipt that a plain ?with= list fires does not. Use it whenever you are looking at a thread for context rather than acting on it — a background poll, a refresh, checking what someone said before you decide to reply. Omit it when you have actually consumed the messages and want them marked read.",
            "type": "string"
          },
          "with": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_chat"
    },
    {
      "description": "Mint a permanent single-file share link (?sig= HMAC; grants read of this one attachment only). Returns {url} as a SERVER-RELATIVE path \u2014 prefix it with the origin you reach this server on to get a link you can paste to someone. The sig carries NO identity and NO expiry: whoever holds the link reads that one blob without signing in, forever, and it cannot be revoked. Mint it for deliverables you meant to hand over; do not paste it anywhere the blob itself would not belong.",
      "inputSchema": {
        "properties": {
          "attachment_id": {
            "type": "string"
          }
        },
        "required": [
          "attachment_id"
        ],
        "type": "object"
      },
      "name": "get_chat_attachment_share_link"
    },
    {
      "description": "List every attachment of a member's conversations (?with=<member_id>; flattened, sender-labelled, newest\u2192oldest).",
      "inputSchema": {
        "properties": {
          "with": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_chat_attachments"
    },
    {
      "description": "Mark a conversation read up to a watermark (reader = verified sub).",
      "inputSchema": {
        "properties": {
          "last_read_ts": {
            "default": 0.0,
            "title": "Last Read Ts",
            "type": "number"
          },
          "peer": {
            "title": "Peer",
            "type": "string"
          }
        },
        "required": [
          "peer"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "post_chat_mark-read"
    },
    {
      "description": "List chat read receipts (?with=<peer>; per-conversation watermark).",
      "inputSchema": {
        "properties": {
          "with": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_chat_reads"
    },
    {
      "description": "Open a reply card: an ask the owner must answer (options \u22644, [0]=AI pick). Auto-binds to your single active task's CURRENT step \u2014 that step (and the task) enters waiting_owner until the owner answers; several lanes of one parallel_group running at once is fine (the lowest order_idx lane carries the card, and the whole task holds either way). If that task has NO resolvable current step the call is REFUSED with 409 and no card is opened: binding the task without a step places no hold, so the task would finish underneath your question and the owner's answer would then be rejected. Fix what the error names \u2014 report the step you are on (update_step_status in_progress), use open_gate with an explicit task_id + step_id, or send bind=\"none\" if the ask is not about the task. With no single clear active task, a plain unbound \u8acb\u793a opens as before. Optional attachments ride the question (same shape as post_chat: {id} from `ocagent upload` / POST /api/chat/attachments, or inline data_b64).",
      "inputSchema": {
        "$defs": {
          "ChatAttachmentInputDTO": {
            "description": "An attachment carried on a posted chat message \u2014 either INLINE BYTES or a\nREFERENCE to a blob already in the shared attachment store.\n\nInline: ``data_b64`` is the bytes, base64-encoded \u2014 a data-URI\n(``data:<mime>;base64,....``) OR bare base64 (the handler strips a\n``data:...;base64,`` prefix if present). ``mime`` and ``filename`` are\noptional: an omitted ``mime`` is sniffed for images (else\n``application/octet-stream``); an omitted ``filename`` defaults for pasted\nimages.\n\nReference: ``id`` names an EXISTING blob (minted by\n``POST /api/chat/attachments`` \u2014 the streaming upload seam that keeps file\nbytes out of a JSON tool call). The stored blob's mime/filename are\nauthoritative: ``filename``/``mime`` sent alongside ``id`` are IGNORED (so\nthe upload response ``{id, mime, filename}`` can be pasted back verbatim).\nAn unknown ``id`` is a 400; carrying BOTH ``id`` and ``data_b64`` is a 400\n(ambiguous intent). An item with neither ``id`` nor ``data_b64`` is a\n400 (T-e2b2: it used to be dropped silently, so a sender that named a file it\nnever sent got a success and the recipient got nothing). The reply-card ANSWER face is\ninline-only: it decodes ``data_b64`` and has never resolved ``id`` references,\nso an item carrying only an ``id`` there is a 400 (T-e2b2 — it used to be\ndropped silently).",
            "properties": {
              "data_b64": {
                "default": "",
                "title": "Data B64",
                "type": "string"
              },
              "filename": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Filename"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Id"
              },
              "mime": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Mime"
              }
            },
            "title": "ChatAttachmentInputDTO",
            "type": "object",
            "additionalProperties": false
          }
        },
        "properties": {
          "attachments": {
            "items": {
              "$ref": "#/$defs/ChatAttachmentInputDTO"
            },
            "title": "Attachments",
            "type": "array"
          },
          "bind": {
            "default": "",
            "description": "Auto-binding opt-out. Omit for the default AUTO binding (bind to your single active task's current step and place the \u7b49\u6211\u56de\u8986 hold). Send \"none\" to declare this ask is NOT about your task \u2014 it opens as a plain unbound \u8acb\u793a whatever work you hold.",
            "title": "Bind",
            "type": "string"
          },
          "body": {
            "default": "",
            "title": "Body",
            "type": "string"
          },
          "kind": {
            "enum": [
              "decision",
              "action"
            ],
            "title": "Kind",
            "type": "string"
          },
          "options": {
            "items": {
              "type": "string"
            },
            "title": "Options",
            "type": "array"
          },
          "summary": {
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "summary",
          "options"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "create_reply_card"
    },
    {
      "description": "List reply cards — LIGHT rows: summary (title) + decision digest only, NO body/options full text (?status=waiting longest-first | answered last-24h; ?limit=N caps the rows). Full card via get_reply_card.",
      "inputSchema": {
        "properties": {
          "limit": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Limit"
          },
          "status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Status"
          }
        },
        "type": "object"
      },
      "name": "list_reply_cards"
    },
    {
      "description": "Read one reply card (full context: options, status, answer).",
      "inputSchema": {
        "properties": {
          "card_id": {
            "type": "string"
          }
        },
        "required": [
          "card_id"
        ],
        "type": "object"
      },
      "name": "get_reply_card"
    },
    {
      "description": "Answer a waiting reply card \u2014 the only positive close.",
      "inputSchema": {
        "$defs": {
          "ChatAttachmentInputDTO": {
            "description": "An attachment carried on a posted chat message \u2014 either INLINE BYTES or a\nREFERENCE to a blob already in the shared attachment store.\n\nInline: ``data_b64`` is the bytes, base64-encoded \u2014 a data-URI\n(``data:<mime>;base64,....``) OR bare base64 (the handler strips a\n``data:...;base64,`` prefix if present). ``mime`` and ``filename`` are\noptional: an omitted ``mime`` is sniffed for images (else\n``application/octet-stream``); an omitted ``filename`` defaults for pasted\nimages.\n\nReference: ``id`` names an EXISTING blob (minted by\n``POST /api/chat/attachments`` \u2014 the streaming upload seam that keeps file\nbytes out of a JSON tool call). The stored blob's mime/filename are\nauthoritative: ``filename``/``mime`` sent alongside ``id`` are IGNORED (so\nthe upload response ``{id, mime, filename}`` can be pasted back verbatim).\nAn unknown ``id`` is a 400; carrying BOTH ``id`` and ``data_b64`` is a 400\n(ambiguous intent). An item with neither ``id`` nor ``data_b64`` is a\n400 (T-e2b2: it used to be dropped silently, so a sender that named a file it\nnever sent got a success and the recipient got nothing). The reply-card ANSWER face is\ninline-only: it decodes ``data_b64`` and has never resolved ``id`` references,\nso an item carrying only an ``id`` there is a 400 (T-e2b2 — it used to be\ndropped silently).",
            "properties": {
              "data_b64": {
                "default": "",
                "title": "Data B64",
                "type": "string"
              },
              "filename": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Filename"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Id"
              },
              "mime": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Mime"
              }
            },
            "title": "ChatAttachmentInputDTO",
            "additionalProperties": false,
            "type": "object"
          }
        },
        "properties": {
          "attachments": {
            "items": {
              "$ref": "#/$defs/ChatAttachmentInputDTO"
            },
            "title": "Attachments",
            "type": "array"
          },
          "card_id": {
            "type": "string"
          },
          "option_idx": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Option Idx"
          },
          "text": {
            "default": "",
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "card_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "answer_reply_card"
    },
    {
      "description": "Revise an answered card's answer (\u91cd\u65b0\u6c7a\u5b9a): stays answered.",
      "inputSchema": {
        "$defs": {
          "ChatAttachmentInputDTO": {
            "description": "An attachment carried on a posted chat message \u2014 either INLINE BYTES or a\nREFERENCE to a blob already in the shared attachment store.\n\nInline: ``data_b64`` is the bytes, base64-encoded \u2014 a data-URI\n(``data:<mime>;base64,....``) OR bare base64 (the handler strips a\n``data:...;base64,`` prefix if present). ``mime`` and ``filename`` are\noptional: an omitted ``mime`` is sniffed for images (else\n``application/octet-stream``); an omitted ``filename`` defaults for pasted\nimages.\n\nReference: ``id`` names an EXISTING blob (minted by\n``POST /api/chat/attachments`` \u2014 the streaming upload seam that keeps file\nbytes out of a JSON tool call). The stored blob's mime/filename are\nauthoritative: ``filename``/``mime`` sent alongside ``id`` are IGNORED (so\nthe upload response ``{id, mime, filename}`` can be pasted back verbatim).\nAn unknown ``id`` is a 400; carrying BOTH ``id`` and ``data_b64`` is a 400\n(ambiguous intent). An item with neither ``id`` nor ``data_b64`` is a\n400 (T-e2b2: it used to be dropped silently, so a sender that named a file it\nnever sent got a success and the recipient got nothing). The reply-card ANSWER face is\ninline-only: it decodes ``data_b64`` and has never resolved ``id`` references,\nso an item carrying only an ``id`` there is a 400 (T-e2b2 — it used to be\ndropped silently).",
            "properties": {
              "data_b64": {
                "default": "",
                "title": "Data B64",
                "type": "string"
              },
              "filename": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Filename"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Id"
              },
              "mime": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Mime"
              }
            },
            "title": "ChatAttachmentInputDTO",
            "additionalProperties": false,
            "type": "object"
          }
        },
        "properties": {
          "attachments": {
            "items": {
              "$ref": "#/$defs/ChatAttachmentInputDTO"
            },
            "title": "Attachments",
            "type": "array"
          },
          "card_id": {
            "type": "string"
          },
          "option_idx": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Option Idx"
          },
          "text": {
            "default": "",
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "card_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "reanswer_reply_card"
    },
    {
      "description": "Mark a waiting card expired (\u6a19\u70ba\u904e\u671f): its author, owner, or admin agent; terminal, not an answer.",
      "inputSchema": {
        "properties": {
          "card_id": {
            "type": "string"
          }
        },
        "required": [
          "card_id"
        ],
        "type": "object"
      },
      "name": "expire_reply_card"
    },
    {
      "description": "Ingest an agent's context gauge (in-memory; bad body \u2192 400).",
      "inputSchema": {
        "properties": {
          "compaction_count": {
            "default": null,
            "description": "Optional non-negative live Codex compaction count. Identity remains the verified JWT sub.",
            "title": "Compaction Count"
          },
          "context_pct": {
            "default": null,
            "title": "Context Pct"
          },
          "rate_limits": {
            "additionalProperties": true,
            "title": "Rate Limits",
            "type": "object"
          }
        },
        "additionalProperties": false,
        "type": "object"
      },
      "name": "ingest_agent_context"
    },
    {
      "description": "Ingest warden telemetry (hardware/limits/tokens/cost/self_update).",
      "inputSchema": {
        "properties": {
          "account": {
            "default": null,
            "title": "Account"
          },
          "account_label": {
            "default": null,
            "title": "Account Label"
          },
          "binaries": {
            "default": null,
            "description": "Warden heartbeats only — the content fingerprint of the binaries this host is ACTUALLY running: an object keyed by binary name (ocwarden, ocagent, officraft) whose values are that live file's sha256 12-hex prefix. The server diffs them against the build it has embedded to derive the machine's bin_status (current / stale / absent = honest unknown), so a heartbeat that omits this leaves the machine's upgrade state unknowable rather than current. Deliberately a content hash, not a version number.",
            "title": "Binaries"
          },
          "claude": {
            "default": null,
            "description": "Warden heartbeats only — the host's local claude CLI probe. Presence-only, NEVER a secret value. All sub-fields optional: version (string — the resolved claude binary's --version first token; absent = unresolved or probe failed), cred_file (bool — ~/.claude/.credentials.json exists), sub_readable (bool — that file exists AND its claudeAiOauth.subscriptionType is non-blank, the exact readability the account-key derivation needs), keychain (bool — macOS login-keychain item present; ABSENT, not false, on non-darwin where the probe cannot answer). Send it so the roster can tell whether this host can actually run Claude.",
            "title": "Claude",
            "type": "object"
          },
          "command_result": {
            "default": null,
            "title": "Command Result"
          },
          "cost": {
            "default": null,
            "title": "Cost"
          },
          "effort": {
            "default": null,
            "title": "Effort"
          },
          "hardware": {
            "default": null,
            "title": "Hardware"
          },
          "machine": {
            "default": null,
            "title": "Machine"
          },
          "model": {
            "default": null,
            "title": "Model"
          },
        "rate_limits": {
          "default": null,
          "title": "Rate Limits"
        },
        "runtime": {
          "default": null,
          "title": "Runtime"
        },
        "runtimes": {
          "default": null,
          "title": "Runtimes"
        },
        "self_update": {
            "default": null,
            "title": "Self Update"
          },
          "tokens": {
            "default": null,
            "title": "Tokens"
          }
        },
        "additionalProperties": false,
        "type": "object"
      },
      "name": "ingest_telemetry"
    },
    {
      "description": "Monitoring telemetry (roster + context + warden push; honest \u2014 else).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "get_monitoring"
    },
    {
      "description": "Set an account's display name (id = stable tag). Blank name \u2192 422.",
      "inputSchema": {
        "properties": {
          "account_id": {
            "type": "string"
          },
          "display_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Display Name"
          }
        },
        "required": [
          "account_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_account"
    },
    {
      "description": "Set a machine's display name (id = stable host). Blank name \u2192 422.",
      "inputSchema": {
        "properties": {
          "display_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Display Name"
          },
          "machine_id": {
            "type": "string"
          }
        },
        "required": [
          "machine_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_machine"
    },
    {
      "description": "List machines (active wardens): machine_id/display_name/online.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "list_machines"
    },
    {
      "description": "Bootstrap on server: install this machine's warden on the host.",
      "inputSchema": {
        "properties": {
          "machine_id": {
            "type": "string"
          }
        },
        "required": [
          "machine_id"
        ],
        "type": "object"
      },
      "name": "install_warden_on_server_host"
    },
    {
      "description": "Teardown on server: runs `ocwarden teardown` on the SERVER's own host. machine_id is NOT a target — this verb has no way to reach another machine, and naming one is refused (409). The server-local machine is refused too (retiring it revokes credentials fleet-wide). To retire another machine use uninstall_machine then delete_machine; to repair the server host's own warden use install_warden_on_server_host, which runs `install --force` over the existing install.",
      "inputSchema": {
        "properties": {
          "machine_id": {
            "type": "string"
          }
        },
        "required": [
          "machine_id"
        ],
        "type": "object"
      },
      "name": "uninstall_warden_on_server_host"
    },
    {
      "description": "Uninstall a machine: drive the uninstall RPC to its warden.",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "uninstall_machine"
    },
    {
      "description": "Upgrade a machine: kick its warden's self-update NOW.",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "type": "object"
      },
      "name": "upgrade_warden"
    },
    {
      "description": "Delete a machine: soft-delete its warden record (no command sent).",
      "inputSchema": {
        "properties": {
          "member_id": {
            "type": "string"
          }
        },
        "required": [
          "member_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "delete_machine"
    },
    {
      "description": "Read the user-custom additive context block (empty = is_default).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "get_global_context"
    },
    {
      "description": "Whole-block replace of the user-custom additive block ({text}). text is REQUIRED; unknown keys are rejected. Replacing existing content with an empty block needs allow_shrink=true (or use reset_global_context).",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "default": false,
            "title": "Allow Shrink",
            "type": "boolean"
          },
          "text": {
            "default": "",
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "text"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "replace_global_context"
    },
    {
      "description": "Reset the user-custom block to empty (idempotent tombstone).",
      "inputSchema": {
        "properties": {},
        "additionalProperties": false,
        "type": "object"
      },
      "name": "reset_global_context"
    },
    {
      "description": "List role definitions (seed defaults + owner edits).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "list_roles"
    },
    {
      "description": "Size-only overview of EVERY capped document on the station: each role's role definition / insight / DEFAULT lessons bucket, and each task manual's SOP / learnings, as size_chars plus the cap_chars in force for THAT segment (the five segments have five separate caps — each is reported against its own). LIMITATION: lessons is reported for the default bucket only; nothing stops a write from naming another bucket, and such a document spends the same lessons cap yet never appears here. Carries NO document text, so it costs a few hundred bytes. Use it to find which long-lived document is nearly full, then read only that one (get_role / get_insight / get_lessons / get_task_manual). It is the only way to see insight and lessons sizes in bulk — no listing reports those at any price; the manual sizes and caps are also on list_task_manuals ?view=list, and a role definition's size and cap are already on every list_roles row.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "peek_doc_sizes"
    },
    {
      "description": "Create a custom role + its founding member (one pair per call). runtime is claude/codex (absent = claude).",
      "inputSchema": {
        "properties": {
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Effort"
          },
          "member_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Member Name"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Model"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "runtime": {
            "anyOf": [
              {
                "enum": [
                  "claude",
                  "codex"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Runtime"
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "create_role"
    },
    {
      "description": "Read one role definition (unknown \u2192 404).",
      "inputSchema": {
        "properties": {
          "role": {
            "type": "string"
          }
        },
        "required": [
          "role"
        ],
        "type": "object"
      },
      "name": "get_role"
    },
    {
      "description": "Edit a role definition ({name?, definition_md?}; locked names skip).",
      "inputSchema": {
        "properties": {
          "definition_md": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Definition Md"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Name"
          },
          "role": {
            "type": "string"
          }
        },
        "required": [
          "role"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_role"
    },
    {
      "description": "Reset a role definition to seed (idempotent tombstone overlay).",
      "inputSchema": {
        "properties": {
          "role": {
            "type": "string"
          }
        },
        "required": [
          "role"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "reset_role"
    },
    {
      "description": "Hard-delete a custom role + its members (seed \u2192 403; online \u2192 409).",
      "inputSchema": {
        "properties": {
          "role": {
            "type": "string"
          }
        },
        "required": [
          "role"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "delete_role"
    },
    {
      "description": "Read a per-role insight doc - this role's accumulated judgement calls and trade-offs (per role_key). A role may ship with a factory seed, and that seed is PER-ROLE (seeds/insight_<role_key>.md) - today only the assistant has one; a role without one reads genuinely empty until it writes. is_default=true means THIS ROLE has never written its own, whether what you are reading is the factory wording or nothing at all. Separate from the lessons doc on purpose: lessons record what happened and what to do next time, insight records how this role weighs a call. Like lessons, reading is unrestricted: any authenticated identity may read ANY role's insight - it is SEPARATE, not private.",
      "inputSchema": {
        "properties": {
          "role_key": {
            "type": "string"
          }
        },
        "required": [
          "role_key"
        ],
        "type": "object"
      },
      "name": "get_insight"
    },
    {
      "description": "Whole-doc replace of a per-role insight doc ({text}). text is REQUIRED; unknown keys are rejected. Replacing existing content with an empty doc needs allow_shrink=true. Only the role's own agents (and admin) may WRITE it.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "type": "boolean"
          },
          "role_key": {
            "type": "string"
          },
          "text": {
            "type": "string"
          }
        },
        "required": [
          "role_key",
          "text"
        ],
        "type": "object"
      },
      "name": "replace_insight"
    },
    {
      "description": "Patch a per-role insight doc by unique anchors ({edits:[{old,new}]}). Only the role's own agents (and admin) may WRITE it.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "type": "boolean"
          },
          "edits": {
            "items": {
              "properties": {
                "new": {
                  "type": "string"
                },
                "old": {
                  "type": "string"
                }
              },
              "type": "object"
            },
            "type": "array"
          },
          "role_key": {
            "type": "string"
          }
        },
        "required": [
          "role_key",
          "edits"
        ],
        "type": "object"
      },
      "name": "patch_insight"
    },
    {
      "description": "Reset a per-role insight doc back to its factory seed (idempotent tombstone of the overlay) - the counterpart of reset_role on the Duty block. A role with NO seed file (seeds/insight_<role_key>.md) returns 404: there must be a factory version to reset TO. No length cap is applied on this path, matching reset_role - the factory text is part of the product. The overlay you are discarding is retained as a document-history revision, so the reset is recoverable. Only the role's own agents (and admin) may do it.",
      "inputSchema": {
        "properties": {
          "role_key": {
            "type": "string"
          }
        },
        "required": [
          "role_key"
        ],
        "type": "object"
      },
      "name": "reset_insight"
    },
    {
      "description": "Read a per-role lessons doc (per role_key; overlay \u2295 seed).",
      "inputSchema": {
        "properties": {
          "role_key": {
            "type": "string"
          },
          "task_type": {
            "type": "string"
          }
        },
        "required": [
          "role_key",
          "task_type"
        ],
        "type": "object"
      },
      "name": "get_lessons"
    },
    {
      "description": "Whole-doc replace of a per-role lessons doc ({text}). text is REQUIRED; unknown keys are rejected. Replacing existing content with an empty doc needs allow_shrink=true.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "default": false,
            "title": "Allow Shrink",
            "type": "boolean"
          },
          "role_key": {
            "type": "string"
          },
          "task_type": {
            "type": "string"
          },
          "text": {
            "default": "",
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "role_key",
          "task_type",
          "text"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "replace_lessons"
    },
    {
      "description": "Patch a per-role lessons doc by unique anchors ({edits:[{old,new}]}).",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "default": false,
            "title": "Allow Shrink",
            "type": "boolean"
          },
          "edits": {
            "items": {
              "properties": {
                "new": {
                  "default": "",
                  "title": "New",
                  "type": "string"
                },
                "old": {
                  "default": "",
                  "title": "Old",
                  "type": "string"
                }
              },
              "title": "LessonsEditDTO",
              "type": "object",
              "additionalProperties": false
            },
            "title": "Edits",
            "type": "array"
          },
          "role_key": {
            "type": "string"
          },
          "task_type": {
            "type": "string"
          }
        },
        "required": [
          "role_key",
          "task_type",
          "edits"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "patch_lessons"
    },
    {
      "description": "Bounded LIGHT wake snapshot for the caller (identity-locked; recent chat + light open-task rows + size overview — peek sizes first, pull detail via get_task). CHAT is packed newest-first under a CHARACTER BUDGET, not a fixed message count, and stopping at the last message that still fits; each message carries from_name/to_name beside the ids and ts_display (full date + time + zone offset) beside the epoch ts, and folds in its reply card as `card` when it has one — read every ts_display against the top-level `generated_at`. TWO DIFFERENT things can be missing and they are marked DIFFERENTLY: `body_omitted_chars` > 0 means THAT message is here with that many characters COLLAPSED away (another agent's line — the owner's line and your own hand-off notes to yourself are carried in full), re-read it with get_chat; `chat_earlier_omitted` is the other kind and it is a MAYBE, not a fact: that line was cut at a read or budget limit and nothing looked past the cut, so whole messages may be missing from this payload entirely — it is raised even when there is in fact nothing older. Its hint tells you how to CHECK and fetch them. The two are asymmetric ON PURPOSE: the collapse marker is CERTAIN (that message IS here, shortened, exact count); this one is not, and only the fetch settles it. Also carries the STUDIO FLOOR you wake up onto: roster (every member and contractor, each with online/offline status, the machine it runs on, and its duty capped at 1000 chars with `…` marking a cut, the cap applied after the doc's own leading title line is removed — who to ask for help; no insight/learning by owner ruling. Contractors additionally carry their bound task's status, waiting_reason, and step progress (progress_done/progress_total) — members leave these at their zero value; a contractor's 0/0 is ambiguous (a task with no steps yet, or no task at all) and task_status is what tells them apart, non-empty vs empty) and machines (the machine list plus you_are_on, your server-recorded machine binding — never derive it from a hostname).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "resume_summary"
    },
    {
      "description": "Size-only PEEK of the wake snapshot (identity-locked; overview counts/sizes + estimated_total_chars, NO chat/task content). estimated_total_chars is exactly chat_chars + tasks_detail_chars + roster_chars + machines_chars, all four reported in overview: the WHOLE chat block as the snapshot renders it (chat_chars is the rendered block's cost, NOT the sum of the message bodies), plus the plan text its task rows omit and the two studio-floor blocks — what pulling the snapshot actually costs. Step one of the two-step boot: call this FIRST to size resume_summary, then either call resume_summary directly (small) or hand the pull to a cheap sub-agent that returns a digest (large).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "peek_resume_summary_size"
    },
    {
      "description": "List tasks (?executor=&type=&status=, or statuses=[…] for a SET of states — every filter given is ANDed; LIGHT list items — id/task_no/title/type_key/status/priority/executor/creator_id/progress/timestamps/deps + dep_tasks, WITHOUT steps/description/inputs). Ask for the states you actually want (`statuses: [\"not_started\", \"in_progress\"]`) instead of listing everything and filtering yourself — the whole history is a large answer. `statuses` also accepts \"reassigning\", which matches the handover LOCK rather than the status column. `dep_tasks` already carries each blocker's task_no/title/status, so a blocked task needs no follow-up get_task just to name what it is waiting for. Call get_task for a task's full detail (steps, description, inputs).",
      "inputSchema": {
        "properties": {
          "executor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Executor"
          },
          "open": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The literal STRING \"true\" (not a boolean, and any other value is ignored) drops every task that has already reached a terminal status — done, terminated, duplicated — and returns only the work that is still live. Use it instead of enumerating statuses when all you want is \"what is still open\"; it composes with executor/type/status. Omitted, the list carries the closed tasks too, which on a long-lived roster is most of it.",
            "title": "Open"
          },
          "status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Status"
          },
          "statuses": {
            "items": {
              "type": "string"
            },
            "title": "Statuses",
            "type": "array"
          },
          "type": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Type"
          }
        },
        "type": "object"
      },
      "name": "list_tasks"
    },
    {
      "description": "Create a task (dedupes on the manual's key; ad-hoc when type_key omitted). Pass target.kind=outsource to drop the task as an unassigned outsource task (發包); target.runtime is claude/codex (absent = claude). The existing outsource scheduler then spawns workers against the global concurrency cap (outsourceParallelCap) — below the cap it starts immediately, at the cap it queues for capacity and is picked up automatically when a slot frees. No owner-approval card and no per-task approval; the owner may reassign a still-queued task at any time. Caller authorization (正職授權矩陣, T-23cf): an outsource worker may never create a task; a 發包 create is open to any 正職 (owner/admin included); a typed task the manual assigns to member X may be created only by X (owner/admin NOT exempt); an ad-hoc task with a member executor may name only the caller itself unless the caller is owner/admin (a 一般正職 may self-execute or 發包, never assign another member).",
      "inputSchema": {
        "properties": {
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Description"
          },
          "executor_member_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Executor Member Id"
          },
          "inputs": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Inputs"
          },
          "priority": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Priority"
          },
          "target": {
            "properties": {
              "effort": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Effort"
              },
              "kind": {
                "title": "Kind",
                "type": "string"
              },
              "machine": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Machine"
              },
              "model": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Model"
              },
              "runtime": {
                "anyOf": [
                  {
                    "enum": [
                      "claude",
                      "codex"
                    ],
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Runtime"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object",
            "additionalProperties": false
          },
          "title": {
            "title": "Title",
            "type": "string"
          },
          "type_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Type Key"
          }
        },
        "required": [
          "title"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "create_task"
    },
    {
      "description": "Read one task (steps, deps, progress, gate cards).",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "type": "object"
      },
      "name": "get_task"
    },
    {
      "description": "Terminate a task (owner/admin agent; the only non-executor status change).",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "type": "object"
      },
      "name": "terminate_task"
    },
    {
      "description": "Set a task's priority (owner/admin agent any value on any task; the task's own executor any value on their task — frozen INCLUDED, and whoever may freeze may unfreeze, T-6020). The actor who sets frozen is recorded on the task as frozen_by and the field clears when the task leaves frozen. Anyone else is a flat 403. Answers with a bounded receipt (task_id, priority, frozen_by), not the whole task — use get_task when you need the rest.",
      "inputSchema": {
        "properties": {
          "priority": {
            "title": "Priority",
            "type": "string"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "priority"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "set_task_priority"
    },
    {
      "description": "Message the task's executor (owner/admin agent; task context auto-attached).",
      "inputSchema": {
        "$defs": {
          "ChatAttachmentInputDTO": {
            "description": "An attachment carried on a posted chat message \u2014 either INLINE BYTES or a\nREFERENCE to a blob already in the shared attachment store.\n\nInline: ``data_b64`` is the bytes, base64-encoded \u2014 a data-URI\n(``data:<mime>;base64,....``) OR bare base64 (the handler strips a\n``data:...;base64,`` prefix if present). ``mime`` and ``filename`` are\noptional: an omitted ``mime`` is sniffed for images (else\n``application/octet-stream``); an omitted ``filename`` defaults for pasted\nimages.\n\nReference: ``id`` names an EXISTING blob (minted by\n``POST /api/chat/attachments`` \u2014 the streaming upload seam that keeps file\nbytes out of a JSON tool call). The stored blob's mime/filename are\nauthoritative: ``filename``/``mime`` sent alongside ``id`` are IGNORED (so\nthe upload response ``{id, mime, filename}`` can be pasted back verbatim).\nAn unknown ``id`` is a 400; carrying BOTH ``id`` and ``data_b64`` is a 400\n(ambiguous intent). An item with neither ``id`` nor ``data_b64`` is a\n400 (T-e2b2: it used to be dropped silently, so a sender that named a file it\nnever sent got a success and the recipient got nothing). The reply-card ANSWER face is\ninline-only: it decodes ``data_b64`` and has never resolved ``id`` references,\nso an item carrying only an ``id`` there is a 400 (T-e2b2 — it used to be\ndropped silently).",
            "properties": {
              "data_b64": {
                "default": "",
                "title": "Data B64",
                "type": "string"
              },
              "filename": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Filename"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Id"
              },
              "mime": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Mime"
              }
            },
            "title": "ChatAttachmentInputDTO",
            "additionalProperties": false,
            "type": "object"
          }
        },
        "properties": {
          "attachments": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/ChatAttachmentInputDTO"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Attachments"
          },
          "body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Body"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "post_task_message"
    },
    {
      "description": "Submit/replace the workflow plan (done and answered-card steps are kept). T-74f8 交棒閘 (second door): a plan is a step-set write and the task status is DERIVED from the step set, so a plan that leaves EVERY step done CLOSES the task — the same irreversible close the final step report performs. If that task's creator is not its executor and no handover is declared or already real, the replan is refused with 422 BEFORE anything is written (the plan stays fully editable). A plan carries no handoff field, so the way out is to hand over first: create the successor task and point its ``blocked_by`` at this task (the gate then stands aside by itself), or keep one unfinished step and declare the handover on the ``update_step_status`` report that closes it. A replan that still leaves work in the plan is never gated. Answers with a bounded receipt (task_id, steps_total, progress_done, progress_total), not the plan you just sent — use get_task to read the stored step rows back.",
      "inputSchema": {
        "properties": {
          "steps": {
            "items": {
              "properties": {
                "dod": {
                  "title": "Dod",
                  "type": "string"
                },
                "is_gate": {
                  "anyOf": [
                    {
                      "type": "boolean"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Is Gate"
                },
                "name": {
                  "title": "Name",
                  "type": "string"
                },
                "parallel_group": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "default": null,
                  "title": "Parallel Group"
                }
              },
              "required": [
                "name",
                "dod"
              ],
              "type": "object",
              "additionalProperties": false
            },
            "title": "Steps",
            "type": "array"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "steps"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "submit_plan"
    },
    {
      "description": "Correct THIS task's description — the ticket's own text (what the task IS: scope, origin, acceptance). T-e271: until this tool existed there was NO way to change a description after creation — create_task takes one only at birth, submit_plan writes steps, update_task_manual writes the TYPE's manual — so a decision to reword a card had nowhere to land. WHO: the task's own executor, or an admin/owner; anyone else is a flat 403. Creating a task grants NO standing to keep rewriting it — if you handed the task over, it is the new executor's text now. PARTIAL like update_task_manual: omitting `description` changes nothing (a safe no-op), while an explicit \"\" CLEARS it — absent and empty are different on purpose; unknown keys are refused rather than dropped. The write is wholesale within that field: the value replaces whatever was there, so send the full corrected text, not a fragment. ⚠️ Division of labour with update_step_note: the DESCRIPTION says what this task IS (stable); the step NOTE says where a step is RIGHT NOW (volatile, handover-facing) — do not put progress here. A CLOSED task (completed / terminated / duplicated) is STILL editable, on the same terms — unlike its artifact set, which freezes at close. The reason they differ: artifacts are the record of what the task PRODUCED and must stop moving, while a ticket worded wrongly is usually found to be wrong after it closed, and freezing the text would preserve a known falsehood in the permanent record. Every change that actually alters the text retains the previous one as a document version (kind `task_description`, key = the task id) — list it with list_document_history, so a correction is recoverable and the older wording is never simply gone.",
      "inputSchema": {
        "properties": {
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Description"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_task_description"
    },
    {
      "description": "Correct THIS task's title — the one line the task list shows. T-2ebe: until this tool existed a title could never be changed after creation, so a card whose scope was later overturned kept advertising its first wording forever — the description could correct itself, the title could not, and whoever scanned the list saw only the stale half. If you have just corrected a description because the scope moved, ask whether the title still says the same thing. WHO: the task's own executor, or an admin/owner; anyone else is a flat 403. Creating a task grants NO standing to keep rewriting it — if you handed the task over, it is the new executor's title now. PARTIAL like update_task_description: omitting `title` changes nothing (a safe no-op); unknown keys are refused rather than dropped. ⚠️ ONE DIFFERENCE FROM ITS DESCRIPTION TWIN: a blank title (\"\" or only whitespace) is REFUSED with 400, it does NOT clear the field — create_task refuses a blank title too, and a task with no title is a blank row on the list. Surrounding whitespace is trimmed. The write is wholesale within that field: send the full corrected title, not a fragment. A CLOSED task (completed / terminated / duplicated) is STILL editable, on the same terms — a ticket is usually found to be worded wrongly after it closed, and freezing the text would preserve a known falsehood; its artifact set is the opposite and freezes at close. Every change that actually alters the text retains the previous one as a document version (kind `task_title`, key = the task id) — list it with list_document_history, so a correction is recoverable.",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Title"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_task_title"
    },
    {
      "description": "Mark a task duplicated, pointing at the original (executor/owner; terminal). T-74f8 交棒閘 (third door): ``duplicated`` is terminal, so this closes the task; it is never refused. A duplicate's ball is on the ORIGINAL by construction, so the server records that itself — a cross-executor task marked duplicate lands ``handoff='follow_up'`` with ``handoff_task_id`` = ``duplicate_of``. No task_dep edge is added (the original is not blocked by its duplicate).",
      "inputSchema": {
        "properties": {
          "duplicate_of": {
            "title": "Duplicate Of",
            "type": "string"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "duplicate_of"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "mark_duplicate"
    },
    {
      "description": "Report a step status (pending/in_progress/waiting_external/done). Entering waiting_external requires a non-blank waiting_reason (422 otherwise); the task status is derived from its steps. T-74f8 交棒閘: if this report would CLOSE the task (every step done) AND the task's creator is not its executor, the call is REFUSED with 422 unless you say where the ball goes IN THIS SAME CALL — handoff='return_to_creator' (the server opens a durable follow-up task on the creator), handoff='follow_up' + handoff_task_id=<a successor task you already created> (the server hangs this task off it as a dependency, and closing this one releases it), or handoff='none' + handoff_note=<why nothing follows>. The gate stands aside by itself when a non-terminal task already depends on this one — you never see it if the handover is already real. It refuses BEFORE writing anything, so a refused report leaves the plan fully editable: create the successor task, then re-send this same report with the declaration. This is your LAST chance — once the task closes it can never be replanned (submit_plan becomes a permanent 409).",
      "inputSchema": {
        "properties": {
          "handoff": {
            "default": "",
            "description": "Where the ball goes when this report closes the task: 'return_to_creator' | 'follow_up' | 'none'. Read ONLY on the report that would close the task; ignored otherwise.",
            "title": "Handoff",
            "type": "string"
          },
          "handoff_note": {
            "default": "",
            "description": "The handover sentence. REQUIRED for handoff='none' — an un-reasoned \"nothing follows\" is indistinguishable from forgetting, and the note is the only record that a decision was made at all.",
            "title": "Handoff Note",
            "type": "string"
          },
          "handoff_task_id": {
            "default": "",
            "description": "The successor task id. REQUIRED for handoff='follow_up'; the server adds this task to that successor's blocked_by list.",
            "title": "Handoff Task Id",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "step_id": {
            "type": "string"
          },
          "task_id": {
            "type": "string"
          },
          "waiting_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Waiting Reason"
          }
        },
        "required": [
          "task_id",
          "step_id",
          "status"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_step_status"
    },
    {
      "description": "Write this step's working note: where the work stands and what comes next — the field the handover SOP means by 「把還在進行中的工作寫回 task step note」. WHAT TO WRITE — three things, then stop: (1) STATE — one sentence on where this step actually got to; (2) NEXT — one sentence on what whoever takes over does next; (3) EVIDENCE POINTERS — version ids, file and log paths, what you verified YOURSELF versus what you are taking on someone's word, and the limits of what was NOT done. Long narrative does not live here: reasoning and scope belong in the task description, reports and diffs belong on the task as artifacts. The note is the current state — not a report, not an append-only log. Writable in ANY step status (pending, in_progress, waiting_owner, waiting_external, done, superseded), unlike `waiting_reason`, which is locked to waiting_external. Wholesale write: `note` replaces whatever was there and \"\" clears it, so rewrite it as the work moves rather than appending; over 4,000 characters (counted in runes) is refused. Same executor/admin gate as every other task-driving write (403 otherwise). ⚠️ A task auto-closes when its last step is reported done and a closed task 409s — so write the note BEFORE the report that finishes the last step, not after.",
      "inputSchema": {
        "properties": {
          "note": {
            "default": "",
            "description": "The note text. Replaces the step's note wholesale; \"\" clears it. Write what the next session needs: where this step got to, what is in flight, what comes next.",
            "title": "Note",
            "type": "string"
          },
          "step_id": {
            "type": "string"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "step_id",
          "note"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_step_note"
    },
    {
      "description": "Arm a gate step: opens the reply card the owner must answer. Optional attachments ride the question (same shape as post_chat: {id} from `ocagent upload` / POST /api/chat/attachments, or inline data_b64).",
      "inputSchema": {
        "$defs": {
          "ChatAttachmentInputDTO": {
            "description": "An attachment carried on a posted chat message — either INLINE BYTES or a\nREFERENCE to a blob already in the shared attachment store.\n\nInline: ``data_b64`` is the bytes, base64-encoded — a data-URI\n(``data:<mime>;base64,....``) OR bare base64 (the handler strips a\n``data:...;base64,`` prefix if present). ``mime`` and ``filename`` are\noptional: an omitted ``mime`` is sniffed for images (else\n``application/octet-stream``); an omitted ``filename`` defaults for pasted\nimages.\n\nReference: ``id`` names an EXISTING blob (minted by\n``POST /api/chat/attachments`` — the streaming upload seam that keeps file\nbytes out of a JSON tool call). The stored blob's mime/filename are\nauthoritative: ``filename``/``mime`` sent alongside ``id`` are IGNORED (so\nthe upload response ``{id, mime, filename}`` can be pasted back verbatim).\nAn unknown ``id`` is a 400; carrying BOTH ``id`` and ``data_b64`` is a 400\n(ambiguous intent). An item with neither ``id`` nor ``data_b64`` is a\n400 (T-e2b2: it used to be dropped silently, so a sender that named a file it\nnever sent got a success and the recipient got nothing). The reply-card ANSWER face is\ninline-only: it decodes ``data_b64`` and has never resolved ``id`` references,\nso an item carrying only an ``id`` there is a 400 (T-e2b2 — it used to be\ndropped silently).",
            "properties": {
              "data_b64": {
                "default": "",
                "title": "Data B64",
                "type": "string"
              },
              "filename": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Filename"
              },
              "id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Id"
              },
              "mime": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Mime"
              }
            },
            "title": "ChatAttachmentInputDTO",
            "type": "object",
            "additionalProperties": false
          }
        },
        "properties": {
          "attachments": {
            "items": {
              "$ref": "#/$defs/ChatAttachmentInputDTO"
            },
            "title": "Attachments",
            "type": "array"
          },
          "body": {
            "default": "",
            "title": "Body",
            "type": "string"
          },
          "kind": {
            "enum": [
              "decision",
              "action"
            ],
            "title": "Kind",
            "type": "string"
          },
          "options": {
            "items": {
              "type": "string"
            },
            "title": "Options",
            "type": "array"
          },
          "step_id": {
            "type": "string"
          },
          "summary": {
            "title": "Summary",
            "type": "string"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "step_id",
          "kind",
          "summary",
          "options"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "open_gate"
    },
    {
      "description": "Replace the blocking-deps list wholesale.",
      "inputSchema": {
        "properties": {
          "blocked_by": {
            "items": {
              "type": "string"
            },
            "title": "Blocked By",
            "type": "array"
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "blocked_by"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "set_task_deps"
    },
    {
      "description": "Report the task's close-out follow-ups done (terminal tasks only; idempotent).",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "report_task_closeout"
    },
    {
      "description": "Reassign a task to a member or a fresh outsource worker (executor-guarded: a plain agent may reassign only a task it executes; owner/admin drive any task). Caller authorization (正職授權矩陣, T-23cf): owner/admin may hand a task to any active member or 發包 it to a fresh outsource worker; a 一般正職 may only turn its own task into a 發包 (a member target is 403); an outsource worker may not reassign at all. An outsource target uses target.runtime claude/codex (absent = claude), lands the task unassigned for the scheduler to spawn under the global parallel cap, and enters the reassigning handover state.",
      "inputSchema": {
        "properties": {
          "note": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Note"
          },
          "target": {
            "properties": {
              "effort": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Effort"
              },
              "kind": {
                "title": "Kind",
                "type": "string"
              },
              "machine": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Machine"
              },
              "member_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Member Id"
              },
              "model": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Model"
              },
              "runtime": {
                "anyOf": [
                  {
                    "enum": [
                      "claude",
                      "codex"
                    ],
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "title": "Runtime"
              }
            },
            "required": [
              "kind"
            ],
            "type": "object",
            "additionalProperties": false
          },
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "target"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "reassign_task"
    },
    {
      "description": "Take over a reassigned task (the new executor claims it): clears the reassigning lock and fires the predecessor worker. The task status stays derived from its steps; only the lock is cleared. 409 if the task is not under the reassigning lock.",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "claim_task"
    },
    {
      "description": "Register a deliverable (file, image, or link) onto the task's artifact set \u2014 the pinned deliverables shown on the task card. Append-only and repeatable: call it again to pin more. For a file or image, first upload the bytes via the chat-attachments upload to get an attachment id, then call this with kind=file|image and that attachment_id. For a link (e.g. a PR url) call it with kind=link and url \u2014 no upload needed. label is an optional display name (a link title such as \"PR #123\"). Answers with a bounded receipt (task_id, artifact_id, artifact_count), not the whole task.",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          },
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "attachment_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Attachment Id"
          },
          "url": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Url"
          },
          "label": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Label"
          }
        },
        "required": [
          "task_id",
          "kind"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "add_task_artifact"
    },
    {
      "description": "Un-pin (remove) one artifact from a task's artifact set \u2014 the counterpart to add_task_artifact. You may remove artifacts from a task you are the executor of (the owner/assistant may remove on any task). Give the task id and the artifact id (the id returned when it was added, or from get_task's artifacts). The underlying file blob is left intact; only the pin on the card is removed. ONLY WHILE THE TASK IS STILL OPEN: once a task closes (done / terminated / duplicated) its deliverable set is frozen in both directions — remove is refused with the same 409 as add. So swap a deliverable BEFORE you close the task, not after; after the close it can neither be removed nor put back. Answers with a bounded receipt (task_id, artifact_id, artifact_count), not the whole task.",
      "inputSchema": {
        "properties": {
          "task_id": {
            "type": "string"
          },
          "artifact_id": {
            "type": "string"
          }
        },
        "required": [
          "task_id",
          "artifact_id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "remove_task_artifact"
    },
    {
      "description": "List live outsource workers (codename, model, effort, task).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "list_outsource_workers"
    },
    {
      "description": "Read an outsource worker's boot-context preview (owner/admin agent).",
      "inputSchema": {
        "properties": {
          "id": {
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "name": "get_outsource_worker_boot_context"
    },
    {
      "description": "Refocus (\u63db\u624b) an outsource worker (owner/admin agent, online-only else 409).",
      "inputSchema": {
        "properties": {
          "id": {
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "name": "refocus_outsource_worker"
    },
    {
      "description": "Stop (\u505c\u6b62) an outsource worker: kill + hold down (owner/admin agent).",
      "inputSchema": {
        "properties": {
          "id": {
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "name": "stop_outsource_worker"
    },
    {
      "description": "Restart (\u91cd\u555f) an outsource worker that has no live session (owner/admin agent; 409 only when it is actually alive).",
      "inputSchema": {
        "properties": {
          "id": {
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "name": "restart_outsource_worker"
    },
    {
      "description": "Change (\u63db model) an outsource worker's model/effort (owner/admin agent).",
      "inputSchema": {
        "properties": {
          "effort": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional effort quick-pick; null/absent keeps the current effort.",
            "title": "Effort"
          },
          "id": {
            "type": "string"
          },
          "model": {
            "default": "",
            "title": "Model",
            "type": "string"
          },
          "runtime": {
            "anyOf": [
              {
                "enum": [
                  "claude",
                  "codex"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional runtime replacement; null/absent keeps the current runtime.",
            "title": "Runtime"
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "set_outsource_worker_model"
    },
    {
      "description": "List task types (match by display_name/purpose; address by type_key). WITHOUT view=list this returns the FULL manual of every type — every SOP and every learnings blob in one answer, six figures of characters on a real roster. Pass view=list unless you actually need the text.",
      "inputSchema": {
        "properties": {
          "view": {
            "description": "\"list\" = the LIGHT row: type_key / display_name / purpose / updated_ts plus the SIZES of the omitted text (sop_md_chars, learnings_chars) and the caps each is judged against (sop_md_cap_chars, learnings_cap_chars \u2014 the older cap_chars is DEPRECATED: it carries the LEARNINGS cap only and says nothing about sop_md), with sop_md and learnings served empty. Omitted (the default) = the FULL manual of every type, SOP and learnings included. Use \"list\" whenever you are matching or choosing a type rather than executing one: the two answers differ by orders of magnitude, and the light row still tells you how big each manual is, so you can pull the one you picked with get_task_manual.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_task_manuals"
    },
    {
      "description": "Create a task type: pass display_name; the server mints and returns the tm- type_key id (legacy explicit type_key still accepted; duplicate \u2192 409; assignee = owner/admin agent). An outsource assignee may select runtime claude/codex; absent = claude.",
      "inputSchema": {
        "properties": {
          "assignee": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Assignee"
          },
          "display_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Display Name"
          },
          "type_key": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Type Key"
          }
        },
        "additionalProperties": false,
        "type": "object"
      },
      "name": "create_task_manual"
    },
    {
      "description": "Read one task manual (purpose/fields/SOP/learnings/assignee). The SOP and the learnings are judged by two SEPARATE caps: read sop_md_cap_chars and learnings_cap_chars. The older cap_chars is DEPRECATED — it carries the LEARNINGS cap only and says nothing about sop_md, so read sop_md_cap_chars for the SOP.",
      "inputSchema": {
        "properties": {
          "type_key": {
            "type": "string"
          }
        },
        "required": [
          "type_key"
        ],
        "type": "object"
      },
      "name": "get_task_manual"
    },
    {
      "description": "Edit a task manual (partial; content fields agent-editable; assignee = owner/admin agent). An outsource assignee may select runtime claude/codex; absent = claude. Only the fields you name change, so omitting a field is safe — but unknown keys are rejected rather than dropped: the learnings doc goes in learnings (NOT text — that is write_task_learnings' field name). The SOP and the learnings are judged by two SEPARATE caps: read sop_md_cap_chars and learnings_cap_chars. The older cap_chars is DEPRECATED — it carries the LEARNINGS cap only and says nothing about sop_md, so read sop_md_cap_chars for the SOP.",
      "inputSchema": {
        "properties": {
          "assignee": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Assignee"
          },
          "display_name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "The manual's human-facing name — what list_task_manuals shows and what people match on when they pick a type — as opposed to type_key, the immutable id you address it by. Pass it to rename a type without breaking anything that references the key (leading/trailing whitespace is trimmed). Omitted leaves the stored name untouched, like every other field on this partial update.",
            "title": "Display Name"
          },
          "fields": {
            "anyOf": [
              {
                "items": {
                  "properties": {
                    "is_key": {
                      "default": false,
                      "title": "Is Key",
                      "type": "boolean"
                    },
                    "name": {
                      "title": "Name",
                      "type": "string"
                    },
                    "required": {
                      "default": false,
                      "title": "Required",
                      "type": "boolean"
                    }
                  },
                  "required": [
                    "name"
                  ],
                  "type": "object",
                  "additionalProperties": false
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Fields"
          },
          "learnings": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Learnings"
          },
          "purpose": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Purpose"
          },
          "sop_md": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Sop Md"
          },
          "type_key": {
            "type": "string"
          }
        },
        "required": [
          "type_key"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "update_task_manual"
    },
    {
      "description": "Delete a task type (open tasks of the type \u2192 409).",
      "inputSchema": {
        "properties": {
          "type_key": {
            "type": "string"
          }
        },
        "required": [
          "type_key"
        ],
        "type": "object"
      },
      "name": "delete_task_manual"
    },
    {
      "description": "Whole-doc replace of a type's learnings (task-close write-back). The doc text goes in text (NOT learnings — that is update_task_manual's field name); text is REQUIRED and unknown keys are rejected. Wiping existing learnings needs allow_shrink=true.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "default": false,
            "title": "Allow Shrink",
            "type": "boolean"
          },
          "text": {
            "default": "",
            "title": "Text",
            "type": "string"
          },
          "type_key": {
            "type": "string"
          }
        },
        "required": [
          "type_key",
          "text"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "write_task_learnings"
    },
    {
      "description": "Patch a type's learnings by unique anchors ({edits:[{old,new}]}) — the learnings twin of patch_lessons, so the write cost scales with the CHANGE, not the whole (30k-char) doc, and re-typing the whole doc can no longer silently drop content. Edits apply in order; a non-empty old must match the current learnings EXACTLY ONCE (0 or >1 hits reject the WHOLE batch with a 400, zero writes — the unique anchor also acts as an optimistic lock); an empty old appends. Wiping the doc, or shrinking it below a tenth, needs allow_shrink=true.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "default": false,
            "title": "Allow Shrink",
            "type": "boolean"
          },
          "edits": {
            "items": {
              "properties": {
                "new": {
                  "default": "",
                  "title": "New",
                  "type": "string"
                },
                "old": {
                  "default": "",
                  "title": "Old",
                  "type": "string"
                }
              },
              "title": "LessonsEditDTO",
              "type": "object",
              "additionalProperties": false
            },
            "title": "Edits",
            "type": "array"
          },
          "type_key": {
            "type": "string"
          }
        },
        "required": [
          "type_key",
          "edits"
        ],
        "additionalProperties": false,
        "type": "object"
      },
      "name": "patch_task_learnings"
    },
    {
      "description": "READ the retained versions of one editable document (global_context | role_definition | lessons | insight | task_manual_sop | task_manual_learnings): what each version held, when it was replaced and by whom. Read-only, at most the 3 most recent versions, newest first. Putting a version BACK is deliberately not an agent tool — the owner does that from the cockpit — so this cannot change anything. `key` is the document: \"global\" for global_context, the role key, \"<role_key>::<task_type>\" for lessons, the bare role key for insight, the type_key for either task-manual series. A task manual keeps TWO independent series — task_manual_sop holds only sop_md, task_manual_learnings holds only learnings — so editing one never consumes a retained slot of the other, and purpose / fields are not versioned at all. A retired kind is refused with 400 naming the series that replaced it.",
      "inputSchema": {
        "properties": {
          "key": {"type": "string"},
          "kind": {"type": "string"}
        },
        "required": ["kind", "key"],
        "type": "object"
      },
      "name": "list_document_history"
    },
    {
      "description": "READ the SHIPPED DEFAULT of one editable document (global_context | role_definition | lessons | task_manual_sop | task_manual_learnings) — the text a reset would put back, i.e. the 初始版本 entry of that document's version list. Read-only: this tool writes nothing, so reading the default can never replace the live document. Putting the default BACK is deliberately not an agent tool — the owner does that from the cockpit — exactly as with list_document_history. `content` carries the SAME field names a retained version carries, so the same reader can compare a default against the live document. `key` is the document: \"global\" for global_context, the role key, \"<role_key>::<task_type>\" for lessons, the type_key for either task-manual series. 400 for a retired kind (naming the series that replaced it). 404 when this document has no shipped default at all — a role the owner created, a task manual, per-role lessons — which is the same set whose reset the server also 404s.",
      "inputSchema": {
        "properties": {
          "key": {"type": "string"},
          "kind": {"type": "string"}
        },
        "required": ["kind", "key"],
        "type": "object"
      },
      "name": "get_document_seed"
    },
    {
      "description": "List the product-guide docs (slug + title).",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "list_docs"
    },
    {
      "description": "Read one product-guide doc in full (markdown; unknown slug → 404).",
      "inputSchema": {
        "properties": {
          "slug": {
            "type": "string"
          }
        },
        "required": [
          "slug"
        ],
        "type": "object"
      },
      "name": "get_doc"
    },
    {
      "description": "Read the 系統互動 block of the boot context — the shared studio handbook every agent reads at boot. Folded: the owner's edit when one exists, otherwise the shipped factory seed, with is_default saying which of the two you are holding and has_seed saying a factory version exists to go back to. The reply carries size_chars/cap_chars (this document's own size limit, in characters) and is_default/has_seed, so a caller can size an edit before making it and can tell an edited block from the shipped one.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "get_system_interaction"
    },
    {
      "description": "Replace the WHOLE 系統互動 block of the boot context ({text}) — the handbook every agent reads at boot. text is REQUIRED and unknown keys are rejected; emptying a block that had content needs allow_shrink=true. The write is judged against the doc.cap_chars.system_interaction cap unconditionally, and the refusal tells you what you wrote, the cap, and what is already stored. The shipped seed is never overwritten, so reset_system_interaction always gets the factory text back; the version this write replaces is retained in the document history (a save that changes nothing retains nothing). Owner or admin assistant only.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "type": "boolean"
          },
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ],
        "type": "object"
      },
      "name": "replace_system_interaction"
    },
    {
      "description": "Restore the 系統互動 block to the FACTORY text shipped with this build (idempotent tombstone of the overlay). No length cap is applied on this path — the factory text is part of the product, so no setting can block the way back to it. The overlay being discarded is retained in the document history, so the reset is itself recoverable. Owner or admin assistant only.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "name": "reset_system_interaction"
    },
    {
      "description": "Read one runtime's 啟動程序 block — the boot checklist that ends that runtime's boot context. runtime_key is 'claude' or 'codex'; they are separate documents because step 3 of the two says opposite things (claude mounts its own `ocagent listen`, codex must not — the sidecar owns it), so any other value is a 404 rather than a silent fallback to claude. Folded: the owner's edit when one exists, otherwise the shipped factory seed. The reply carries size_chars/cap_chars (this document's own size limit, in characters) and is_default/has_seed, so a caller can size an edit before making it and can tell an edited block from the shipped one.",
      "inputSchema": {
        "properties": {
          "runtime_key": {
            "type": "string"
          }
        },
        "required": [
          "runtime_key"
        ],
        "type": "object"
      },
      "name": "get_boot_sequence"
    },
    {
      "description": "Replace the WHOLE 啟動程序 block of ONE runtime ({runtime_key, text}). runtime_key is 'claude' or 'codex' and the two are separate documents whose step 3 contradicts each other, so writing the wrong one leaves those agents unable to come online — and nothing that never boots reports it. text is REQUIRED and unknown keys are rejected; emptying a block that had content needs allow_shrink=true. Judged against the doc.cap_chars.boot_sequence cap (one cap, both runtimes, each measured on its own text); the refusal tells you what you wrote, the cap, and what is stored. The shipped seed is never overwritten, so reset_boot_sequence always gets the factory text back. Owner or admin assistant only.",
      "inputSchema": {
        "properties": {
          "allow_shrink": {
            "type": "boolean"
          },
          "runtime_key": {
            "type": "string"
          },
          "text": {
            "type": "string"
          }
        },
        "required": [
          "runtime_key",
          "text"
        ],
        "type": "object"
      },
      "name": "replace_boot_sequence"
    },
    {
      "description": "Restore ONE runtime's 啟動程序 block to the FACTORY text shipped with this build (idempotent tombstone of the overlay). runtime_key is 'claude' or 'codex'; anything else is a 404. No length cap is applied on this path — the factory text is part of the product, so no setting can block the way back to it, which is what makes this the recovery route when a bad edit has stopped agents from booting. The overlay being discarded is retained in the document history. Owner or admin assistant only.",
      "inputSchema": {
        "properties": {
          "runtime_key": {
            "type": "string"
          }
        },
        "required": [
          "runtime_key"
        ],
        "type": "object"
      },
      "name": "reset_boot_sequence"
    }
  ]
}
